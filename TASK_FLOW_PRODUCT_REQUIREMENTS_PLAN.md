# نقشه عملیاتی اصلاح جریان تسک و پایه تحصیلی

## هدف سند

این سند نتیجه بررسی نه درخواست محصول در کد فعلی است و باید مرجع مشترک ایجنت‌های پیاده‌سازی، بازبینی و تست باشد. هدف، تعریف دقیق حالت فعلی، حالت مطلوب، دامنه تغییر، ترتیب اجرا و معیار پذیرش است. این سند برنامه اجراست و در این مرحله هیچ‌یک از تغییرات محصول را پیاده‌سازی نمی‌کند.

## وضعیت اجرا

- [x] مرحله ۱ - تثبیت lifecycle و permission (موارد ۵ و ۶) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [x] مرحله ۲ - قرارداد curriculum چندگفتاری و گزارش (موارد ۳ و ۷) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [x] مرحله ۳ - workflow مشترک و history ناوبری (موارد ۲ و ۴) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [x] مرحله ۴ - ثبت سریع برنامه هفتگی (مورد ۹) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [x] مرحله ۵ - range picker مشترک (مورد ۸) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [x] مرحله ۶ - عنوان فارغ‌التحصیل و migration (مورد ۱) - تکمیل در ۱۴۰۵/۰۵/۲۱
- [ ] مرحله ۷ - تست end-to-end و regression نهایی - کنترل خودکار تکمیل، سناریوهای تعاملی نقش‌محور باقی‌مانده

### گزارش مرحله ۱

- انتقال روز برای تسک فعال خود دانش‌آموز اکنون فقط تاریخ را تغییر می‌دهد و lifecycle را به `PENDING` برمی‌گرداند؛ دیگر وارد ناقصی‌ها نمی‌شود.
- انتقال به ناقصی‌ها فقط `status: INCOMPLETE` و `completed: null` را ذخیره می‌کند و تاریخ یا `detailsCompleted` را تغییر نمی‌دهد.
- draft از انتقال به ناقصی‌ها محافظت شده است. انتقال روز تسک مشاور و تسک پایان‌یافته نیز تا تصمیم صریح محصول مسدود است.
- تسک مشاور در UI دانش‌آموز فقط تکمیل، انتقال به ناقصی‌ها و ثبت عملکرد واقعی دارد؛ انتقال روز، ویرایش محتوا و حذف نمایش داده نمی‌شوند.
- API تغییر تسک مشاور توسط دانش‌آموز را به lifecycle و actual metrics محدود می‌کند؛ payloadهای تاریخ، target و curriculum رد می‌شوند.
- `TaskActionDialog` عملیات async را await می‌کند، loading دارد و هنگام خطای API باز می‌ماند و پیام واقعی نشان می‌دهد.
- پنل مشاور status، منشأ تسک، زمان واقعی و تست واقعی را از داده مشترک نمایش می‌دهد.
- کنترل کیفیت: ۶ تست Vitest پاس، ESLint فایل‌های مرحله بدون خطا و `tsc --noEmit` بدون خطا.

### گزارش مرحله ۲

- رابطه صریح `TaskTopic` به Prisma اضافه شد و migration، `topicId`های legacy را به‌صورت idempotent backfill می‌کند. ستون تکی در دوره گذار برای سازگاری داده‌های قبلی حفظ شده است.
- picker، store، task service و APIهای single create، batch create، list، get و patch قرارداد `topicIds` را حفظ می‌کنند؛ PATCH حذف/جایگزینی relation را اتمیک انجام می‌دهد.
- validation سرور همه topic IDها را از نظر وجود، subject، chapter و ناسازگاری با topic mode بررسی می‌کند و summary را از داده curriculum می‌سازد.
- API metadata مرتب گفتارها را برمی‌گرداند و formatter مشترک همه گفتارها و بازه صفحه را نمایش می‌دهد؛ draft نیز summary انتخاب‌شده را پنهان نمی‌کند.
- picker برای ویرایش task موجود، grade/chapter/topics/page range را از payload hydrate می‌کند.
- گزارش فصل‌محور subject tree را از union همان endpoint eligibility ثبت تسک دریافت و تطبیق را با `subjectId`, `chapterId` و `topicIds` انجام می‌دهد؛ fallback نام فقط برای داده legacy باقی مانده است.
- در چندگفتاری، مجموع فصل هر task یک بار حساب می‌شود و سهم جزئی topicها تقسیم می‌شود تا زمان و تست چندبرابر نشوند. درس‌های بدون task نیز در selector باقی می‌مانند.
- migration `20260812172500_add_task_topics` روی PostgreSQL لوکال اعمال و وضعیت schema تأیید شد.
- کنترل کیفیت: ۲۴ تست Vitest پاس، ESLint فایل‌های مرحله بدون خطا و `tsc --noEmit` بدون خطا.

### گزارش مرحله ۳

- back داخلی picker از گفتار به فصل و از فصل به پایه فقط navigation state را عقب می‌برد و selection payload مرحله بالاتر را حذف نمی‌کند.
- hydration مرحله ۲ باعث شده بازگشت و ورود دوباره به picker در تکمیل draft، فصل و همه گفتارهای قبلی را حفظ کند.
- قرارداد selection بین ثبت تکی و تکمیل draft مشترک باقی مانده و هر دو از `TaskSubjectPicker` و payload چندگفتاری واحد استفاده می‌کنند.
- `ManualEntrySheet` اکنون workflow واحد با modeهای `create` و `complete-draft` است؛ `TaskDetailsDialog` فقط wrapper نازک آن است و فرم تخت مستقل حذف شده است.
- mode تکمیل draft همه curriculum IDs، topicها، صفحات، فعالیت و اهداف را hydrate می‌کند، ثبت اولیه را پنهان می‌کند و metadata سازنده، تاریخ و order را تغییر نمی‌دهد.

### گزارش مرحله ۴

- selector ظاهری حوزه از برنامه هفتگی حذف شد و لیست ثبت سریع union بدون duplicate دروس مجاز کنکور و نهایی است.
- `fieldType` هر گزینه به‌صورت metadata داخلی حل می‌شود: هدف نهایی اولویت نهایی و اهداف کنکور/هر دو اولویت کنکور دارند؛ fallback حوزه دیگر فقط برای درس‌های غیرمشترک است.
- ثبت سریع همچنان draft واقعی می‌سازد و تکمیل آن از workflow مشترک مرحله ۳ انجام می‌شود.

### گزارش مرحله ۵

- component مشترک `PersianDateRangePicker` بر پایه تقویم شمسی پروژه ساخته شد و Analytics و WeeklyPlanner هر دو از آن استفاده می‌کنند.
- بازه معکوس normalize، دو سر بازه inclusive، شمارنده روز و سقف ۶۰ روز در component واحد اعمال می‌شود.
- inputهای بومی میلادی از نسخه موبایل و دسکتاپ Analytics حذف شدند و هر دو به state مشترک متصل‌اند.

### گزارش مرحله ۶

- `Grade`، onboarding، تنظیمات، مدیریت مؤسسه، seed، mock و مستندات کد به «فارغ‌التحصیل» تغییر کردند؛ curriculum همچنان فقط سه پایه دبیرستان دارد.
- migration `20260812174000_rename_graduate_grade` رکوردهای قدیمی را به مقدار جدید تبدیل می‌کند و روی PostgreSQL لوکال با موفقیت اجرا شد.
- جست‌وجوی کد هیچ رخداد قدیمی خارج از migration و متن خود plan پیدا نکرد و eligibility کنکور همچنان همه پایه‌های رشته را می‌پذیرد.

### گزارش مرحله ۷ - کنترل کیفیت نهایی

- `npm test`: هر ۲۴ تست در ۳ فایل پاس شدند.
- `npm run lint`: بدون error؛ یک warning قدیمی و نامرتبط درباره unused eslint-disable در `CurriculumWizard.tsx` باقی مانده است.
- `npm run build`: build تولید Next.js، TypeScript، Prisma generate و تولید ۲۱ صفحه با موفقیت کامل شد.
- `prisma migrate status`: هر ۶ migration روی PostgreSQL لوکال اعمال شده و schema به‌روز است.
- smoke test سرور توسعه: پاسخ `HTTP 200` از `http://localhost:3000` دریافت شد.
- باقی‌مانده: سناریوهای تعاملی role-based ماتریس روی حساب دانش‌آموز/مشاور و viewport موبایل/دسکتاپ باید به‌صورت دستی یا Playwright اجرا شوند؛ repo فعلی زیرساخت E2E ندارد.

## برداشت نهایی از درخواست‌ها

1. عنوان وضعیت تحصیلی «پشت کنکوری» در تمام نقاط پروفایل کاربر به «فارغ‌التحصیل» تغییر کند.
2. در تمام مسیرهای چندمرحله‌ای ثبت یا تکمیل تسک، دکمه «قبلی» فقط یک مرحله واقعی به عقب برود و کاربر را به ابتدای جریان نبرد.
3. پس از ثبت تسک، کارت و جزئیات تسک باید انتخاب دقیق کاربر، به‌ویژه گفتار یا گفتارهای انتخاب‌شده، را نمایش دهند و فقط عنوان کلی فصل کافی نیست.
4. جریان «تکمیل جزئیات پیش‌نویس» باید همان منطق، مراحل، اعتبارسنجی و خروجی جریان «افزودن تکی تسک از برنامه من» را داشته باشد.
5. دانش‌آموز فقط روی تسکی که خودش ساخته است امکان ویرایش، انتقال به روز دیگر، انتقال به ناقصی‌ها و حذف کامل دارد. روی تسک ساخته‌شده توسط مشاور فقط سه اقدام مجاز است: تیک تکمیل، انتقال به ناقصی‌ها و تنظیم زمان واقعی مطالعه/تعداد تست واقعی. مشاور نیز باید وضعیت تکمیل یا انتقال به ناقصی‌های تسک‌های خود را مشاهده کند.
6. انتقال روز و انتقال به ناقصی‌ها باید از نظر lifecycle، تاریخ، محل نمایش و بازخورد UI اصلاح شوند تا تسک دقیقاً در مقصد مورد انتظار دیده شود.
7. گزارش تفکیک درس و فصل باید همان دروس قابل استفاده در ثبت تسک را بشناسد و بر مبنای شناسه‌های curriculum گزارش درست بسازد.
8. انتخاب بازه دلخواه در گزارش باید دقیقاً از همان تجربه تقویم شمسی و منطق بازه موجود در برنامه هفتگی استفاده کند.
9. برنامه هفتگی باید فقط ثبت سریع درس را نشان دهد، تفکیک ظاهری کنکور/نهایی را حذف کند و تکمیل جزئیات را به workflow مشترک افزودن تسک بسپارد.

## تصمیم‌های دامنه‌ای

- «فارغ‌التحصیل» یک مقدار پروفایل دانش‌آموز است، نه پایه محتوای درسی. ثابت‌های ماتریس برنامه درسی در `src/lib/subjects-types.ts`، `SubjectFormModal` و `CurriculumWizard` باید همچنان فقط `دهم`، `یازدهم` و `دوازدهم` باشند.
- برای کاربر فارغ‌التحصیل، تسک کنکوری می‌تواند از پایه‌های دبیرستان رشته او انتخاب شود؛ رفتار فعلی `getEligibleTaskSubject` برای حوزه کنکور همین رویکرد را دارد. تسک نهایی برای فارغ‌التحصیل نیازمند تصمیم محصول جداگانه است، چون منطق فعلی آن را به تطابق دقیق پایه پروفایل محدود می‌کند و پایه‌ای به نام فارغ‌التحصیل در محتوای درسی وجود ندارد. تا تعیین این تصمیم، نباید با افزودن پایه جعلی به curriculum مسئله را دور زد.
- وضعیت «پیش‌نویس» (`DRAFT`, جزئیات هنوز تکمیل نشده) با «ناقص» (`INCOMPLETE`, تسک معتبر اما انجام‌نشده/به تعویق‌افتاده) یکی نیست و نباید ادغام شود.
- تسک مشاور پس از تکمیل یا انتقال به ناقصی‌ها همچنان `createdBy: 'advisor'` باقی می‌ماند و جزئیات هدف آن توسط دانش‌آموز قابل تغییر نیست.
- انتقال روز، حذف کامل و ویرایش محتوایی فقط برای تسک `createdBy: 'student'` مجاز است. تسک `createdBy: 'advisor'` از این عملیات محافظت می‌شود.
- زمان واقعی مطالعه و تعداد تست واقعی، گزارش عملکرد دانش‌آموز هستند و دانش‌آموز باید بتواند آن‌ها را برای تسک مشاور ثبت کند؛ این مقادیر با زمان و تست هدف تعیین‌شده توسط مشاور متفاوت‌اند.
- تغییر lifecycle تسک مشاور توسط دانش‌آموز باید در داده مشترک ذخیره شود تا مشاور بدون ثبت رویداد موازی یا state محلی جداگانه، وضعیت جدید را در پنل خود مشاهده کند.

## ۱. جایگزینی «پشت کنکوری» با «فارغ‌التحصیل»

### حالت فعلی

- نوع `Grade` در `src/lib/types.ts` مقدار `پشت کنکوری` را دارد.
- گزینه و برچسب آن در `src/components/onboarding/OnboardingWizard.tsx` وجود دارد.
- گزینه مشابه در `src/components/settings/SettingsView.tsx` و `src/components/institute/InstituteStudents.tsx` وجود دارد.
- داده‌های seed و mock در `prisma/seed.ts` و `src/lib/constants/mockData.ts` این مقدار را ذخیره می‌کنند.
- توضیح API ثبت‌نام و کامنت schema نیز مقدار قدیمی را مستند کرده‌اند.
- ماتریس محتوای درسی عمداً فقط سه پایه دبیرستان را می‌پذیرد و مقدار قدیمی را ندارد.

### حالت مطلوب

- در UI، typeها، داده‌های نمونه، seedها و مستندات کد فقط عنوان `فارغ‌التحصیل` دیده و تولید شود.
- رکوردهای موجود دیتابیس با مقدار `پشت کنکوری` به `فارغ‌التحصیل` مهاجرت کنند تا فیلتر، ویرایش پروفایل و نمایش اطلاعات ناسازگار نشود.
- پایه‌های curriculum بدون تغییر و سه‌گانه باقی بمانند.

### برنامه پیاده‌سازی

1. مقدار union و آرایه‌های گزینه پروفایل را در فایل‌های بالا جایگزین کنید.
2. seed، mock، توضیح route ثبت‌نام و کامنت schema را هماهنگ کنید؛ متن آزاد یادداشت مشاور فقط اگر نقش label سیستمی دارد تغییر کند.
3. یک migration داده‌ای idempotent اضافه کنید که `User.grade = 'پشت کنکوری'` را به `فارغ‌التحصیل` تبدیل کند.
4. همه رخدادهای عبارت قدیمی را جست‌وجو کنید و موارد عمدی مربوط به migration را از موارد فراموش‌شده تفکیک کنید.
5. رفتار انتخاب درس کنکور برای کاربر فارغ‌التحصیل را با API `subjects/for-task` و `getEligibleTaskSubject` تست کنید.

### معیار پذیرش

- ثبت‌نام، تنظیمات و فرم مؤسسه گزینه «فارغ‌التحصیل» دارند و گزینه قدیمی ندارند.
- کاربر قدیمی پس از migration با مقدار جدید نمایش داده می‌شود.
- ساختار محتوای درسی پایه‌ای به نام «فارغ‌التحصیل» ایجاد نمی‌کند.
- ساخت تسک کنکوری برای فارغ‌التحصیل همچنان درس‌ها و پایه‌های دبیرستان رشته او را ارائه می‌دهد.

## ۲. بازگشت دقیق به مرحله قبلی در تمام جریان‌های ثبت تسک

### حالت فعلی

- `ManualEntrySheet` سه مرحله والد دارد و دکمه قبلی با `step - 1` کار می‌کند.
- داخل مرحله انتخاب درس، `TaskSubjectPicker` ناوبری جداگانه درس، پایه، فصل و گفتار دارد.
- بازگشت از گفتار به فصل، selection خروجی را تا سطح subject پاک می‌کند؛ بنابراین state والد دیگر جزئیات مرحله قبلی را نگه نمی‌دارد.
- بازگشت از فصل به پایه و تغییر درس نیز بخش بزرگی از state را reset می‌کند.
- `TaskDetailsDialog` جریان مرحله‌ای صریح مشابه افزودن تسک ندارد؛ بنابراین مفهوم «قبلی» بین ثبت تکی و تکمیل پیش‌نویس یکسان نیست.
- مسیر مشاور (`TaskModal`) نیز از picker مشترک استفاده می‌کند اما wrapper و مراحل آن متفاوت است.

### حالت مطلوب

- history منطقی مسیر حفظ شود: حوزه ← درس ← پایه ← فصل ← گفتار ← جزئیات.
- هر بار فشردن «قبلی» فقط به node قبلی همین history برگردد.
- انتخاب‌های مرحله جلو تا وقتی کاربر انتخاب مرحله بالاتر را عوض نکرده حفظ شوند؛ صرف بازگشت نباید گفتار، بازه صفحه یا فصل را نابود کند.
- رفتار در ثبت تکی، تکمیل پیش‌نویس و هر wrapper دیگری که برای ثبت تسک استفاده می‌شود یکسان باشد.

### برنامه پیاده‌سازی

1. state ناوبری picker را صریح کنید، برای مثال `subject | grade | chapter | topics`، و عملیات `goBack` واحد بسازید.
2. state ناوبری را از payload انتخاب جدا کنید؛ عقب رفتن فقط view را تغییر دهد و پاک‌سازی payload تنها پس از انتخاب جایگزین انجام شود.
3. wrapper مشترک جریان تسک، back مرحله والد و back داخلی picker را هماهنگ کند: ابتدا عمق داخلی picker یک مرحله عقب برود و فقط در ریشه picker به مرحله والد بازگردد.
4. `ManualEntrySheet` و جریان تکمیل پیش‌نویس را روی همین state machine مشترک سوار کنید.
5. تست تعاملی برای هر transition و حفظ state بنویسید.

### معیار پذیرش

- از صفحه گفتار، یک بار «قبلی» فصل‌ها را نشان می‌دهد؛ بار بعد پایه‌ها؛ بار بعد درس‌ها؛ بدون پرش به انتخاب حوزه.
- از جزئیات مطالعه، «قبلی» به آخرین وضعیت انتخاب مبحث برمی‌گردد.
- رفت و برگشت بدون تغییر انتخاب، مقادیر قبلی را حفظ می‌کند.
- تغییر عمدی درس یا حوزه، فقط داده‌های وابسته و نامعتبر را پاک می‌کند.
- رفتار موبایل و دسکتاپ یکسان است.

## ۳. نمایش جزئیات دقیق تسک پس از افزودن

### حالت فعلی

- picker در `TaskSubjectPicker` یک `displayText` شامل فصل و همه گفتارهای انتخاب‌شده تولید می‌کند.
- کلاینت این متن را در `ManualEntrySheet.buildTask` به فیلد `topic` می‌دهد.
- مدل `Task` فقط یک `topicId` دارد و هنگام انتخاب چند گفتار، فقط ID اولین گفتار در `topicId` قرار می‌گیرد.
- APIهای POST/PATCH متن ارسالی کلاینت را منبع حقیقت نمی‌دانند و `topic` را با خروجی `validateTaskCurriculum` جایگزین می‌کنند؛ این خروجی فقط عنوان همان یک گفتار یا فصل را برمی‌گرداند.
- `TaskCard` فقط وقتی `detailsCompleted` درست باشد `task.topic` را نمایش می‌دهد. پیش‌نویس، با وجود داشتن فصل/گفتار انتخاب‌شده، جزئیات را پنهان می‌کند.
- نتیجه این است که متن دقیق انتخاب کاربر، مخصوصاً انتخاب چند گفتار، پس از round-trip API از بین می‌رود یا فقط عنوان کلی/یک عنوان محدود نمایش داده می‌شود.

### حالت مطلوب

- تسک ذخیره‌شده ساختار انتخاب‌شده را بدون اتکا به رشته دست‌ساز کلاینت حفظ کند.
- کارت تسک و دیالوگ جزئیات، حداقل درس، فصل، گفتار یا گفتارهای انتخاب‌شده و در صورت وجود بازه صفحات را نمایش دهند.
- پیش‌نویس نیز خلاصه محتوای انتخاب‌شده را نمایش دهد، حتی اگر فعالیت و زمان هنوز تکمیل نشده باشد.
- متن نمایشی از داده معتبر curriculum ساخته شود و با rename فصل/گفتار ناسازگار نماند.

### برنامه پیاده‌سازی

1. ابتدا قرارداد محصول برای چندگفتاری را تثبیت کنید. پیشنهاد: رابطه چندبه‌چند `TaskTopic` به جای `topicId` تکی؛ نگهداری فقط یک ID با قابلیت multi-select سازگار نیست.
2. migration schema و backfill برای تبدیل `topicId` فعلی به رابطه جدید ایجاد کنید. برای سازگاری موقت API، فقط در دوره migration می‌توان `topicId` اول را نیز برگرداند.
3. `validateTaskCurriculum` را طوری توسعه دهید که همه topic IDها را از نظر تعلق به subject/chapter بررسی و summary ساختاریافته برگرداند.
4. POST، PATCH، batch و parser/typeهای Task را با قرارداد جدید هماهنگ کنید.
5. یک formatter مشترک برای summary بسازید و در `TaskCard` و `TaskDetailsDialog` استفاده کنید؛ نمایش summary را به `detailsCompleted` وابسته نکنید.
6. نمایش بازه صفحات را نیز از `pageStart/pageEnd` اضافه کنید.

### معیار پذیرش

- انتخاب یک گفتار بعد از ثبت، همان گفتار را روی کارت نشان می‌دهد.
- انتخاب چند گفتار بعد از reload نیز همه گفتارها را نشان می‌دهد.
- انتخاب فقط فصل، عنوان فصل را نشان می‌دهد.
- پیش‌نویس عنوان فصل/گفتار را نشان می‌دهد ولی فعالیت/زمان خالی را به‌عنوان جزئیات تکمیل‌شده جا نمی‌زند.
- rename یک گفتار باعث نمایش عنوان قدیمی و stale در تسک نمی‌شود.

## ۴. یکسان‌سازی تکمیل جزئیات پیش‌نویس با افزودن تکی از «برنامه من»

### حالت فعلی

- مرجع مطلوب فعلی `ManualEntrySheet` است: انتخاب حوزه، انتخاب درس/پایه/فصل/گفتار، سپس فعالیت، زمان و تست؛ با امکان ثبت اولیه یا ثبت کامل.
- تکمیل پیش‌نویس در `TaskDetailsDialog` یک دیالوگ تخت و جداست؛ picker و همه فیلدها همزمان نمایش داده می‌شوند.
- اعتبارسنجی متفاوت است: `TaskDetailsDialog` وجود `displayText` را اجباری می‌کند، در حالی که ثبت تکی `chapterId` را مبنای quick/full save می‌گیرد.
- صفحه شروع/پایان در فرم تکمیل پیش‌نویس هم داخل picker و هم به شکل input مستقل وجود دارد و دو منبع state می‌سازد.
- مقدار `fieldType` در تکمیل پیش‌نویس قابل طی کردن در همان منطق مرحله اول نیست.
- initialization picker از task موجود کامل نیست؛ state داخلی picker از `chapterId/topicId` ورودی hydrate نمی‌شود و ممکن است کاربر دوباره از ابتدای انتخاب حرکت کند.

### حالت مطلوب

- افزودن تکی و تکمیل پیش‌نویس یک workflow component و یک validation/build contract داشته باشند.
- تفاوت فقط mode باشد: `create` برای تسک جدید و `complete-draft` برای به‌روزرسانی تسک موجود.
- mode تکمیل پیش‌نویس داده موجود را hydrate کند، همان مراحل را نشان دهد و در پایان همان payload کامل را با حفظ `id`, `createdBy`, `createdById`, `date`, `order` ذخیره کند.
- صفحه‌ها فقط یک منبع state داشته باشند؛ ترجیحاً picker مشترک.

### برنامه پیاده‌سازی

1. منطق فرم `ManualEntrySheet` را به یک component مشترک مانند `TaskFlow` با modeهای `create` و `complete-draft` تبدیل کنید؛ از کپی کردن منطق خودداری شود.
2. توابع مشترک validation و payload construction تعریف کنید تا create و update قواعد یکسان داشته باشند.
3. hydration کامل picker را پیاده کنید: subject، grade، chapter، همه topicها و page range باید از task موجود بازیابی شوند.
4. `TaskDetailsDialog` را به wrapper نازک workflow مشترک تبدیل یا حذف کنید.
5. پس از تکمیل پیش‌نویس، lifecycle را به `PENDING`, `detailsCompleted: true`, `completed: null` تغییر دهید؛ provenance و جایگاه تسک تغییر نکند.
6. خطاهای API را مانند ثبت تکی به کاربر منتقل کنید و catch عمومی بدون پیام واقعی را حذف کنید.

### معیار پذیرش

- ظاهر، ترتیب مراحل، دکمه قبلی، فیلدها و validation در هر دو مسیر یکسان است.
- پیش‌نویس با اطلاعات قبلی باز می‌شود و کاربر مجبور به انتخاب مجدد درس/فصل نیست.
- تکمیل پیش‌نویس هیچ تسک جدیدی نمی‌سازد و metadata سازنده را تغییر نمی‌دهد.
- payload نهایی دو مسیر برای انتخاب‌های یکسان، به‌جز ID و mode عملیات، یکسان است.
- بازه صفحه فقط در یک محل و یک state مدیریت می‌شود.

## ۵. تفکیک عملیات تسک دانش‌آموز و تسک مشاور

### حالت فعلی

- در `TaskCard` تسک pending ساخته‌شده توسط مشاور دکمه تکمیل و دکمه تنظیمات/ثبت بخشی دارد.
- دکمه عملیات با علامت X فقط وقتی `task.createdBy === 'student'` است نمایش داده می‌شود. این محدودیت برای انتقال روز و حذف درست است، اما باعث شده گزینه مجاز «انتقال به ناقصی‌ها» نیز برای تسک مشاور در دسترس نباشد.
- `PlanView.handleMoveToIncomplete` و تب ناقصی‌ها از قبل وجود دارند.
- API PATCH برای دانش‌آموز روی تسک مشاور فقط lifecycle fields شامل `status`, `completed`, `actualTimeMinutes`, `actualTestCount` را مجاز می‌داند. تغییر `date` فعلاً 403 می‌شود.
- `canModifyTask` برای دانش‌آموز فقط تسکی را قابل تغییر/حذف می‌داند که `createdBy === 'student'` باشد؛ بنابراین DELETE تسک مشاور نیز 403 می‌شود.
- `PlanView.handleActionDelete` پیش از API حذف تسک مشاور را صریحاً منع می‌کند. این رفتار باید حفظ شود.
- مشاور از مسیر مشاهده تسک‌های دانش‌آموز به داده مشترک دسترسی دارد، اما UI مشاور باید statusهای `COMPLETED` و `INCOMPLETE` و actual metrics را به‌وضوح نمایش دهد و پس از تغییر دانش‌آموز refresh شود.

### حالت مطلوب

- تسک ساخته‌شده توسط خود دانش‌آموز منوی عملیات کامل شامل ویرایش، انتقال به روز مشخص، انتقال به ناقصی‌ها و حذف کامل داشته باشد.
- تسک ساخته‌شده توسط مشاور برای دانش‌آموز فقط سه اقدام داشته باشد: «تکمیل»، «انتقال به ناقصی‌ها» و «تنظیمات/ثبت عملکرد واقعی».
- روی تسک مشاور هیچ دکمه یا مسیر UI برای ویرایش محتوا، حذف یا انتقال به روز مشخص نمایش داده نشود و API نیز این درخواست‌ها را 403 کند.
- انتقال تسک مشاور به ناقصی‌ها آن را با `status: 'INCOMPLETE'`, `completed: null` و بدون تغییر `date`, `detailsCompleted`, جزئیات هدف یا provenance ذخیره کند.
- تنظیمات تسک مشاور فقط `actualTimeMinutes` و `actualTestCount` را تغییر دهد و به `targetTimeMinutes` یا `targetTestCount` دست نزند.
- مشاور در فهرست و جزئیات دانش‌آموز، وضعیت تیک‌خورده یا ناقص و در صورت ثبت، زمان و تست واقعی را مشاهده کند.

### برنامه پیاده‌سازی

1. برای تسک دانش‌آموز همان `TaskActionDialog` کامل را حفظ کنید.
2. برای تسک مشاور یک action اختصاصی «انتقال به ناقصی‌ها» در کنار تکمیل و تنظیمات اضافه کنید؛ دیالوگ شامل انتقال روز یا حذف نباید برای آن باز شود.
3. منع موجود در `handleActionDelete` و DELETE API برای تسک مشاور حفظ و با تست تثبیت شود.
4. مجوز PATCH تسک مشاور action-based باقی بماند و فقط `status`, `completed`, `actualTimeMinutes`, `actualTestCount` را برای دانش‌آموز مالک بپذیرد؛ `date` و تمام فیلدهای محتوایی رد شوند.
5. `handleMoveToIncomplete` برای تسک مشاور فقط lifecycle را تغییر دهد و جزئیات هدف را دست‌کاری نکند.
6. UI مشاور در `AdvisorStudentDetail` یا فهرست متناظر، status و actual metrics تسک را از داده API نمایش دهد و پس از بازگشت/refresh آخرین state را دریافت کند.
7. تست‌های API مثبت برای تکمیل، ناقصی و actual metrics و تست‌های منفی برای PATCH date، DELETE و ویرایش جزئیات هدف اضافه کنید.

### معیار پذیرش

- دانش‌آموز روی تسک خودش به ویرایش، انتقال روز، ناقصی‌ها و حذف دسترسی دارد.
- دانش‌آموز روی تسک مشاور فقط تکمیل، ناقصی‌ها و تنظیم عملکرد واقعی را می‌بیند.
- انتقال به ناقصی‌ها تسک را در تب ناقصی‌ها نمایش می‌دهد و سازنده «مشاور» و جزئیات هدف را حفظ می‌کند.
- حذف، ویرایش یا انتقال روز تسک مشاور در UI در دسترس نیست و درخواست مستقیم API نیز 403 می‌شود.
- ثبت تنظیمات تسک مشاور فقط زمان و تست واقعی را تغییر می‌دهد.
- مشاور وضعیت `COMPLETED` یا `INCOMPLETE` و actual metrics ثبت‌شده توسط دانش‌آموز را مشاهده می‌کند.
- تکمیل و ثبت بخشی همچنان بدون regression کار می‌کنند.

## ۶. اصلاح منطق انتقال روز و انتقال به ناقصی‌ها

### حالت فعلی

- `handleMoveDate` برای هر تسک غیر `DRAFT` علاوه بر تغییر تاریخ، وضعیت را به `INCOMPLETE` تغییر می‌دهد. در نتیجه «انتقال به روز مشخص» عملاً هم‌زمان تسک را وارد ناقصی‌ها می‌کند.
- نمای روزانه فقط وضعیت‌های `PENDING`, `COMPLETED`, `SKIPPED` را نمایش می‌دهد و `INCOMPLETE` را نمایش نمی‌دهد؛ بنابراین تسک منتقل‌شده در روز مقصد دیده نمی‌شود و کاربر تصور می‌کند انتقال کار نکرده است.
- تب ناقصی‌ها تمام تسک‌های `INCOMPLETE` را بدون توجه به تاریخ نشان می‌دهد. پس انتقال روز و انتقال به ناقصی‌ها از نظر نتیجه ظاهری با هم مخلوط شده‌اند.
- `handleMoveToIncomplete` برای تسک دانش‌آموز `detailsCompleted: true` می‌فرستد، حتی اگر تسک یک draft واقعی با فعالیت/زمان خالی باشد؛ API برای وضعیت غیر `DRAFT` جزئیات معتبر می‌خواهد و این مسیر می‌تواند 400 شود.
- دیالوگ عملیات callbackهای async را await نمی‌کند و بلافاصله بسته می‌شود؛ خطا یا rollback در خود دیالوگ نمایش داده نمی‌شود.
- empty state و عنوان تب ناقصی‌ها تفاوت «انجام‌نشده»، «منتقل‌شده» و «پیش‌نویس تکمیل‌نشده» را به‌صورت شفاف نشان نمی‌دهد.

### حالت مطلوب

- «انتقال به روز مشخص» فقط زمان‌بندی را تغییر دهد: تسک معتبر pending در روز مقصد به‌عنوان `PENDING` دیده شود.
- «انتقال به ناقصی‌ها» فقط lifecycle را به `INCOMPLETE` ببرد و تاریخ را بی‌دلیل تغییر ندهد.
- draft ناقص یا باید در تب پیش‌نویس باقی بماند یا ابتدا جزئیاتش تکمیل شود؛ نباید با جعل `detailsCompleted: true` به ناقصی‌ها منتقل شود.
- عملیات تا پاسخ موفق API loading داشته باشد؛ در خطا دیالوگ بسته نشود و UI به state قبلی برگردد.
- تب ناقصی‌ها summary صحیح مبحث، تاریخ اولیه/فعلی و منشأ مشاور یا خودم را نمایش دهد.

### برنامه پیاده‌سازی

1. transition table واحد برای `DRAFT`, `PENDING`, `INCOMPLETE`, `COMPLETED`, `SKIPPED` تعریف و در UI/API تست شود.
2. `handleMoveDate` را به `{ date: newDate, status: 'PENDING', completed: null }` برای تسک فعال تبدیل کنید؛ برای completed/skipped تصمیم حفظ یا reset وضعیت باید صریح باشد. پیشنهاد: انتقال یک تسک پایان‌یافته ابتدا confirmation و سپس reset به pending.
3. `handleMoveToIncomplete` را به `{ status: 'INCOMPLETE', completed: null }` محدود کنید و `detailsCompleted` را دست‌کاری نکنید.
4. برای `DRAFT` گزینه انتقال به ناقصی‌ها را مخفی/غیرفعال کنید و آن را در پیش‌نویس‌ها نگه دارید.
5. callbackهای `TaskActionDialog` را Promise-based و await کنید؛ loading، خطا و جلوگیری از double-submit اضافه شود.
6. فیلترهای `PlanView`, شمارنده تقویم، برنامه هفتگی و تب ناقصی‌ها را با transition table هماهنگ کنید.

### معیار پذیرش

- انتقال یک pending از امروز به فردا، آن را فردا در «برنامه روز» نشان می‌دهد و وارد ناقصی‌ها نمی‌کند.
- انتقال به ناقصی‌ها، تسک را از برنامه روز حذف و در تب ناقصی‌ها با تمام جزئیات نمایش می‌دهد.
- draft با جزئیات خالی از مسیر ناقصی‌ها خطای API یا state متناقض تولید نمی‌کند.
- شکست شبکه دیالوگ را نمی‌بندد و تسک پس از rollback در محل قبلی دیده می‌شود.
- انتقال روز فقط برای تسک خود دانش‌آموز در دسترس است؛ انتقال به ناقصی‌ها برای هر دو نوع تسک کار می‌کند.
- تسک مشاور پس از ناقص شدن در پنل مشاور نیز با وضعیت صحیح دیده می‌شود.

## ۷. اصلاح گزارش تفکیک درس و فصل

### حالت فعلی

- افزودن تسک از `/api/subjects/for-task` استفاده می‌کند. در حوزه کنکور، این endpoint درس‌های همه پایه‌های فعال رشته را برمی‌گرداند.
- گزارش فصل‌محور از `/api/subjects?include=tree&grade=<پایه دقیق>&major=<رشته>` استفاده می‌کند؛ این قرارداد با endpoint افزودن تسک یکسان نیست و برای فارغ‌التحصیل یا تسک کنکوری پایه‌های دیگر می‌تواند هیچ درسی برنگرداند.
- گزارش سپس `GradeSubject` را دوباره با `g.grade === userGrade` پیدا می‌کند؛ در حالی که تسک کنکوری می‌تواند به پایه دیگری مرتبط باشد.
- تطبیق تسک و درس با `t.subject === subject.name` و `t.fieldType === (subject.isKonkur ? 'کنکور' : 'نهایی')` انجام می‌شود. یک Subject می‌تواند در جریان نهایی نیز قابل استفاده باشد، پس تبدیل boolean `isKonkur` به fieldType یکتا، تسک‌های معتبر را حذف می‌کند.
- breakdown قدیمی فصل‌ها با متن `topic` تطبیق می‌دهد و breakdown جدید با ID؛ هم‌زمانی دو منطق باعث خروجی متفاوت می‌شود.
- پیام «دروس فعال برای پایه و رشته شما یافت نشد» خطای قرارداد fetch را با واقعاً تعریف‌نبودن درس یکی نشان می‌دهد.

### حالت مطلوب

- منبع دروس گزارش و افزودن تسک یک قرارداد eligibility مشترک داشته باشند.
- تطبیق اصلی با `subjectId`, `chapterId`, `topicId/topicIds`, `topicModeId` انجام شود، نه نام متنی یا استنتاج fieldType از `isKonkur`.
- گزارش برای تسک‌های کنکوری همه پایه‌های انتخاب‌شده دانش‌آموز را نشان دهد و برای نهایی پایه مرتبط را رعایت کند.
- دروس تعریف‌شده حتی اگر در بازه انتخابی فعالیت تکمیل‌شده ندارند در selector دیده شوند؛ آمارشان صفر باشد، نه اینکه کل درس ناپدید شود.
- empty state بین «هیچ درس مجازی تعریف نشده»، «داده‌ای در بازه نیست» و «خطای دریافت اطلاعات» تمایز بگذارد.

### برنامه پیاده‌سازی

1. endpoint یا service مشترک eligibility برای task picker و analytics تعریف کنید؛ query باید fieldType/grade/major و tree مورد نیاز را پشتیبانی کند.
2. گزارش را بر اساس `subjectId` گروه‌بندی کنید و fallback نام را فقط برای داده‌های legacy بدون ID نگه دارید.
3. شرط `fieldType === (subject.isKonkur ? ...)` را حذف و fieldType انتخابی گزارش را مستقیماً روی taskها اعمال کنید.
4. برای کنکور GradeSubject متناظر با `chapterId` تسک را مبنا قرار دهید، نه فقط `user.grade`.
5. دو پیاده‌سازی موازی `SubjectChapterBreakdown` و `ChapterCentricReport` را روی aggregator مشترک سوار کنید یا یکی را حذف کنید.
6. تست fixture شامل درس کنکوری پایه دیگر، درس نهایی، فارغ‌التحصیل، subject rename و task legacy اضافه کنید.

### معیار پذیرش

- هر درسی که در افزودن تسک قابل انتخاب است در selector گزارش نیز دیده می‌شود.
- انتخاب درس بدون تسک تکمیل‌شده، ساختار فصل‌ها را با آمار صفر نشان می‌دهد.
- تسک تکمیل‌شده با `subjectId/chapterId/topicId` در درس و فصل صحیح جمع می‌شود.
- فیلتر «همه/کنکوری/نهایی» تسک‌ها را بر اساس `task.fieldType` تفکیک می‌کند.
- کاربر فارغ‌التحصیل برای تسک‌های کنکوری پیام اشتباه «درسی تعریف نشده» نمی‌بیند.

## ۸. یکسان‌سازی انتخاب بازه گزارش با برنامه هفتگی

### حالت فعلی

- گزارش برای بازه دلخواه از دو input بومی `type="date"` استفاده می‌کند که میلادی و وابسته به مرورگر هستند.
- برنامه هفتگی دکمه‌های «از/تا»، تاریخ فارسی، تقویم شمسی inline، جابه‌جایی ماه، انتخاب خودکار تاریخ پایان و شمارنده روز دارد.
- این UI در دو layout موبایل و دسکتاپ گزارش نیز جداگانه تکرار شده است.
- `filterTasksForReport` بازه را inclusive اعمال می‌کند، اما در صورت نبود یکی از تاریخ‌ها عملاً محدودیت تاریخ ندارد و feedback واضحی ارائه نمی‌شود.

### حالت مطلوب

- گزارش دقیقاً همان component انتخاب بازه برنامه هفتگی را استفاده کند، نه نسخه شبیه‌سازی‌شده جداگانه.
- تاریخ‌ها شمسی نمایش داده و به ISO محلی استاندارد برای فیلتر تبدیل شوند.
- شروع و پایان، حالت active picker، تعویض ماه، شمارنده روز، normalize معکوس بودن بازه و محدودیت طول رفتار یکسان داشته باشند.
- تا کامل نشدن هر دو تاریخ، گزارش بازه قبلی را حفظ کند یا حالت «بازه را کامل کنید» نشان دهد؛ نباید ناگهان همه داده‌ها را نمایش دهد.

### برنامه پیاده‌سازی

1. range picker و `MiniMonthCalendar` را از `WeeklyPlanner` به component مشترک مانند `PersianDateRangePicker` استخراج کنید.
2. component فقط `{ start: Date | null, end: Date | null }` و callback استاندارد ارائه دهد؛ تبدیل ISO در helper مشترک انجام شود.
3. هر دو نسخه موبایل/دسکتاپ گزارش را به یک state و یک component وصل کنید.
4. قواعد inclusive، swap start/end، حداکثر ۶۰ روز و timezone محلی را تست کنید.

### معیار پذیرش

- ظاهر و رفتار انتخاب بازه گزارش با برنامه هفتگی یکسان است.
- تاریخ شمسی انتخاب‌شده دقیقاً تسک‌های همان روزهای ابتدا و انتها را شامل می‌شود.
- انتخاب بازه معکوس به‌درستی normalize می‌شود.
- بازه ناقص باعث نمایش همه تاریخچه یا نمودار گمراه‌کننده نمی‌شود.

## ۹. بازطراحی ثبت سریع در برنامه هفتگی

### حالت فعلی

- بالای برنامه هفتگی selector آشکار «کنکور/نهایی» وجود دارد و فهرست درس‌ها با تغییر آن دوباره fetch می‌شود.
- کلیک روی درس بلافاصله یک `DRAFT` فقط با subject و fieldType می‌سازد.
- سپس کلیک روی chip، `TaskDetailsDialog` متفاوت از جریان افزودن فعلی را باز می‌کند.
- در نتیجه ثبت هفتگی سه منطق جدا دارد: انتخاب field، ثبت سریع draft و تکمیل جزئیات با فرم قدیمی.
- chip فقط نام درس و «تکمیل جزئیات» را نشان می‌دهد و summary دقیق مرحله تکمیل را ندارد.

### حالت مطلوب

- هیچ selector یا badge ظاهری «کنکور/نهایی» در پنل برنامه هفتگی و modal ثبت سریع نمایش داده نشود.
- کاربر برای هر روز فقط فهرست یکپارچه درس‌ها را ببیند و با یک کلیک draft همان درس را ثبت کند.
- `fieldType` همچنان برای قرارداد داده لازم است اما به شکل داخلی و قطعی تعیین شود؛ نباید با حدس مبهم ذخیره شود.
- پس از ثبت سریع، تکمیل جزئیات دقیقاً از workflow مشترک افزودن تکی فعلی استفاده کند: درس/پایه/فصل/گفتار، فعالیت، زمان و تست، back history و validation یکسان.
- ثبت سریع باید در حد انتخاب درس باقی بماند و کاربر را مجبور به تکمیل فوری نکند.

### تصمیم فنی برای fieldType پنهان

- پیشنهاد: endpoint ثبت سریع فهرست eligibility یکپارچه برگرداند و برای هر گزینه، fieldType معتبر داخلی را مشخص کند.
- اگر یک درس هم برای کنکور و هم نهایی معتبر است، حذف UI نباید به انتخاب تصادفی منجر شود. رفتار پیشنهادی: اولویت داخلی بر اساس `user.goal` و در حالت «هر دو» یک قاعده محصولی ثابت یا انتخاب در مرحله جزئیات، بدون نمایش برچسب روی لیست سریع.
- این نکته پیش از پیاده‌سازی نیازمند تأیید محصول است؛ حذف صرف selector و hard-code کردن `کنکور` قابل قبول نیست.

### برنامه پیاده‌سازی

1. selector fieldType و تمام label/badgeهای آن را از UI برنامه هفتگی حذف کنید.
2. endpoint فهرست سریع را برای بازگرداندن union بدون تکرار درس و metadata داخلی لازم طراحی کنید.
3. draft سریع را فقط با subject معتبر، روز انتخابی و fieldType حل‌شده ایجاد کنید.
4. `TaskDetailsDialog` داخل WeeklyPlanner را با workflow مشترک بخش ۴ جایگزین کنید.
5. chip هفتگی را با summary مشترک تسک هماهنگ و عملیات حذف/تکمیل را از نظر loading و خطا ایمن کنید.

### معیار پذیرش

- در برنامه هفتگی هیچ متن یا کنترل «کنکور/نهایی» نمایش داده نمی‌شود.
- کاربر روز را انتخاب و با یک کلیک درس را به‌صورت draft ثبت می‌کند.
- درس‌های مجاز بدون duplicate و بدون حذف اشتباه در لیست سریع دیده می‌شوند.
- تکمیل draft هفتگی همان مراحل، back، validation و payload افزودن تکی را دارد.
- بعد از reload، fieldType داخلی تسک معتبر است و گزارش می‌تواند آن را درست تفکیک کند.

## ترتیب پیشنهادی اجرا

1. **تثبیت lifecycle و permission:** موارد ۵ و ۶، چون انتقال و حذف باید پیش از اتصال UIهای دیگر قرارداد روشنی داشته باشند.
2. **زیرساخت قرارداد curriculum و گزارش:** موارد ۳ و ۷، چون نمایش تسک و گزارش باید از شناسه‌های یکسان استفاده کنند.
3. **workflow مشترک و history ناوبری:** موارد ۲ و ۴، شامل استخراج فرم مشترک create/complete-draft.
4. **ثبت سریع برنامه هفتگی:** مورد ۹، پس از آماده شدن workflow مشترک.
5. **range picker مشترک:** مورد ۸، قابل اجرای مستقل با هماهنگی فایل WeeklyPlanner و AnalyticsView.
6. **تغییر عنوان و migration داده پایه:** مورد ۱؛ تست گزارش و eligibility فارغ‌التحصیل پس از آن تکرار شود.
7. **تست end-to-end و regression نهایی:** همه سناریوها روی موبایل و دسکتاپ.

## تقسیم کار پیشنهادی بین ایجنت‌ها

- **ایجنت داده/API:** طراحی و migration رابطه چندگفتاری، validation curriculum، permissionهای action-based، DELETE/PATCH، eligibility مشترک و migration مقدار پایه.
- **ایجنت workflow frontend:** ساخت state machine مشترک، hydration، back history و یکسان‌سازی create/complete-draft.
- **ایجنت task card/lifecycle:** نمایش summary، عملیات محدود تسک مشاور، عملیات کامل تسک خود دانش‌آموز و اصلاح نمایش انتقال/ناقصی‌ها در پنل دانش‌آموز و مشاور.
- **ایجنت analytics:** aggregator مبتنی بر ID، eligibility دروس و empty-stateهای گزارش.
- **ایجنت date/weekly UI:** range picker مشترک و ثبت سریع بدون نمایش fieldType.
- **ایجنت تست/بازبینی:** تست component/API/E2E و کنترل regression مجوزها، گزارش، reload و responsive UI.

هر ایجنت پیش از تغییر باید فایل‌های دارای تغییرات همزمان را بررسی کند. فایل‌های `TaskSubjectPicker.tsx`, `ManualEntrySheet.tsx`, `TaskDetailsDialog.tsx`, `TaskCard.tsx`, `PlanView.tsx`, routeهای tasks و schema نقاط تلاقی اصلی هستند و نباید بدون هماهنگی در چند worktree جداگانه همزمان بازنویسی شوند.

## ماتریس تست نهایی

| سناریو | انتظار |
| --- | --- |
| ثبت‌نام با وضعیت فارغ‌التحصیل | مقدار جدید ذخیره و نمایش داده شود |
| کاربر قدیمی پشت کنکوری | پس از migration به فارغ‌التحصیل تبدیل شود |
| ثبت تسک با یک گفتار | همان فصل و گفتار بعد از reload نمایش داده شود |
| ثبت تسک با چند گفتار | همه گفتارها بعد از reload نمایش داده شوند |
| ثبت اولیه پیش‌نویس | summary مبحث نمایش داده شود؛ فعالیت و زمان تکمیل‌شده تلقی نشوند |
| تکمیل پیش‌نویس | همان workflow ثبت تکی با مقادیر hydrate‌شده باز شود |
| back از گفتار | فقط به فصل برگردد و selection حفظ شود |
| back از جزئیات | به آخرین مرحله مبحث برگردد، نه ابتدای فرم |
| ناقص کردن تسک خود دانش‌آموز | به ناقصی‌ها منتقل شود |
| ناقص کردن تسک مشاور | به ناقصی‌ها منتقل شود و metadata مشاور حفظ شود |
| تغییر روز تسک خود دانش‌آموز | در روز مقصد به‌عنوان تسک فعال نمایش داده شود |
| تلاش برای تغییر روز تسک مشاور | UI ارائه نکند و API با 403 رد کند |
| تلاش برای حذف تسک مشاور | UI ارائه نکند و API با 403 رد کند |
| تلاش دانش‌آموز برای ویرایش محتوای تسک مشاور | UI ارائه نکند و API با 403 رد کند |
| تکمیل یا ثبت بخشی تسک مشاور | مانند قبل موفق باشد |
| مشاهده تسک تیک‌خورده در پنل مشاور | status تکمیل و actual metrics صحیح نمایش داده شود |
| مشاهده تسک منتقل‌شده به ناقصی در پنل مشاور | status ناقص و جزئیات هدف قبلی نمایش داده شود |
| درس قابل انتخاب در افزودن تسک | در selector گزارش نیز موجود باشد |
| درس بدون فعالیت در بازه | با ساختار فصل و آمار صفر نمایش داده شود |
| گزارش کاربر فارغ‌التحصیل | دروس کنکوری پایه‌های مرتبط را نشان دهد |
| بازه دلخواه گزارش | با تقویم شمسی مشترک و دو سر inclusive فیلتر شود |
| ثبت سریع هفتگی | بدون نمایش کنکور/نهایی، draft درس را ثبت کند |
| تکمیل draft هفتگی | workflow یکسان با افزودن تکی داشته باشد |

## دستورات کنترل کیفیت

پس از پیاده‌سازی کامل اجرا شوند:

```bash
npm test
npm run lint
npm run build
```

علاوه بر تست‌های خودکار، سناریوهای ماتریس بالا باید در viewport موبایل و دسکتاپ به‌صورت دستی یا E2E اجرا شوند. بررسی صرف typecheck برای مشکلات history، hydration و نمایش جزئیات کافی نیست.

## موارد نیازمند تصمیم محصول پیش از نهایی‌سازی

1. آیا کاربر «فارغ‌التحصیل» مجاز به ساخت تسک حوزه «نهایی» است؟ اگر بله، پایه محتوای مورد نظر چگونه تعیین می‌شود؟ پیشنهاد فنی این است که هنگام حوزه نهایی، پایه curriculum جداگانه از کاربر گرفته شود، نه اینکه «فارغ‌التحصیل» به‌عنوان پایه محتوا ثبت شود.
2. برای نمایش چند گفتار، آیا همه عناوین باید مستقیم روی کارت دیده شوند یا کارت خلاصه‌ای مانند «۳ گفتار» نشان دهد و متن کامل در جزئیات باز شود؟ حداقل معیار این سند حفظ همه داده‌ها و دسترسی به متن کامل است.
3. آیا «ناقص» کردن تسک مشاور باید برای مشاور notification یا رویداد audit ایجاد کند؟ درخواست فعلی فقط lifecycle و UI را الزام می‌کند و notification را شامل نمی‌شود.
4. اگر یک درس هم در حوزه کنکور و هم نهایی مجاز باشد، ثبت سریع هفتگی بدون نمایش selector باید کدام `fieldType` را ذخیره کند؟ پیشنهاد: حل بر اساس هدف کاربر و در حالت «هر دو» انتخاب در مرحله تکمیل جزئیات، بدون نمایش badge در لیست سریع.
5. انتقال یک تسک `COMPLETED` یا `SKIPPED` به روز دیگر باید وضعیت پایان‌یافته را حفظ کند یا آن را به `PENDING` برگرداند؟ پیشنهاد سند: با confirmation به `PENDING` برگردد تا انتقال معنای برنامه‌ریزی مجدد داشته باشد.

تا زمان پاسخ به این موارد، ایجنت‌ها می‌توانند زیرساخت و سناریوهای بدون ابهام را پیاده کنند، اما نباید برای تسک نهایی فارغ‌التحصیل، تعیین fieldType مبهم، انتقال تسک پایان‌یافته یا notification رفتار محصولی جدید حدس بزنند.
