# گزارش جامع باگ‌یابی پروژه Reval (روال)

**کامیت بررسی‌شده:** `dc83d51594e1d0c420a74edc74d34e588c321979` (feat: connect super admin and Liara deployment)
**شاخه:** `main`
**مخزن:** https://github.com/ArmanGBG/reval
**تاریخ بررسی:** بر اساس کلون محلی در `/home/z/reval-analysis`
**روش:** بررسی دستی کد + تحلیل خودمختار ۴ بخش (API routes / Frontend / Lib services / Config-Build)

> **نکته مهم:** این گزارش فقط باگ‌ها را **پیدا** کرده و توضیح می‌دهد. هیچ کدی اصلاح/نوشته نشده است. راه‌حل‌ها به‌صورت مفهومی و کوتاه پیشنهاد شده‌اند.

---

## فهرست

- [خلاصه اجرایی](#خلاصه-اجرایی)
- [نحوه خواندن این گزارش](#نحوه-خواندن-این-گزارش)
- [بخش ۱ — باگ‌های بحرانی (CRITICAL)](#بخش-۱--باگهای-بحرانی-critical)
- [بخش ۲ — باگ‌های با اهمیت بالا (HIGH)](#بخش-۲--باگهای-با-اهمیت-بالا-high)
- [بخش ۳ — باگ‌های متوسط (MEDIUM)](#بخش-۳--باگهای-متوسط-medium)
- [بخش ۴ — باگ‌های کم‌اهمیت (LOW)](#بخش-۴--باگهای-کماهمیت-low)
- [جدول خلاصه و اولویت‌بندی](#جدول-خلاصه-و-اولویتبندی)
- [توصیه‌های نهایی](#توصیههای-نهایی)

---

## خلاصه اجرایی

پروژه Reval یک پلتفرم برنامه‌ریزی تحصیلی فارسی (Next.js 16 + Prisma + PostgreSQL) با چهار نقش (دانش‌آموز، مشاور، مدیر مؤسسه، مدیر کل) است. بررسی کد در کامیت فعلی **چندین باگ بحرانی** نشان می‌دهد که مهم‌ترین آن‌ها عبارتند از:

1. **تغییر نام فایل middleware به `proxy.ts`** — این یعنی Next.js اصلاً آن را به‌عنوان middleware تشخیص نمی‌دهد، در نتیجه هیچ درخواست API‌ای محافظت نمی‌شود و **تمام مسیرهای محافظت‌شده پس از ورود، ۴۰۱ برمی‌گردانند**. این یعنی کل اپلیکیشن بعد از لاگین از کار می‌افتد.
2. **فایل `.env` اشتباه در گیت commit شده** — `DATABASE_URL` به یک مسیر SQLite محلی اشاره می‌کند ولی schema از PostgreSQL استفاده می‌کند. در هر محیطی Prisma نمی‌تواند وصل شود.
3. **IDOR در ویرایش آزمون** — یک مشاور می‌تواند هر دانش‌آموزی (حتی دانش‌آموزان مشاور دیگر) را به آزمون خودش اضافه کند.
4. **اسکریپت seed کل جدول task را پاک می‌کند** — `scripts/seed-from-excel.js` با `deleteMany({})` تمام تسک‌های همه دانش‌آموزان را حذف می‌کند.
5. **عدم تنظیم `AUTH_SECRET` در production** — باعث crash کل API می‌شود.

> **هشدار:** باگ شماره ۱ (middleware) به‌تنهایی باعث می‌شود اپلیکیشن در حالت فعلی **کاملاً غیرقابل‌استفاده** باشد (هر درخواست بعد از لاگین ۴۰۱ می‌دهد). این باگ باید اول از همه برطرف شود.

---

## نحوه خواندن این گزارش

هر باگ با این ساختار آمده است:

- **فایل / خط:** مسیر فایل و شماره خط (یا خطوط) دقیق
- **دسته:** نوع باگ (امنیت / منطق / عملکرد / داده / پیکربندی ...)
- **چه خطایی می‌دهد (به زبان ساده):** توصیف تجربه کاربر یا رفتار اشتباه
- **راه‌حل پیشنهادی:** توصیف کوتاه و مفهومی (بدون کد)

---

# بخش ۱ — باگ‌های بحرانی (CRITICAL)

این باگ‌ها باعث می‌شوند اپلیکیشن کلاً کار نکند، امنیت کاملاً نقض شود، یا داده‌ها از بین بروند.

---

### C1. تغییر نام `middleware.ts` به `proxy.ts` → تمام APIها پس از لاگین ۴۰۱ می‌دهند

- **فایل / خط:** `src/proxy.ts:4` (تابع `proxy`) + `src/lib/api-auth.ts:29-31` (خواندن `x-user-id`)
- **دسته:** پیکربندی / احراز هویت (بحرانی)
- **تأیید شده با بررسی دستی و تاریخچه گیت.**

**چه خطایی می‌دهد (ساده):**
در کامیت `8470056` فایل `src/middleware.ts` به `src/proxy.ts` و نام تابع `middleware` به `proxy` تغییر کرده است. Next.js **فقط** زمانی کد را به‌عنوان middleware اجرا می‌کند که:
1. فایل دقیقاً `middleware.ts` (یا `middleware.js`) در ریشه یا `src/` باشد، و
2. تابعی به نام `middleware` (default یا named) export کند.

چون هیچ‌کدام رعایت نشده، **هیچ middleware‌ای اجرا نمی‌شود**. نتیجه:
- header به‌نام `x-user-id` که تابع `requireAuth` می‌خواند، **هرگز ست نمی‌شود**.
- تابع `getUserIdFromRequest` همیشه `null` برمی‌گرداند.
- `requireAuth` همیشه با پیام «احراز هویت لازم است» و status 401 برمی‌گردد.
- **تمام** مسیرهای محافظت‌شده (تسک‌ها، آزمون‌ها، پیام‌ها، دروس، مدیریت مؤسسه، ...) بعد از لاگین کار نمی‌کنند. فقط مسیرهای `/api/auth/me`، `login`، `register`، `otp/*` و `logout` کار می‌کنند چون خودشان توکن را بررسی می‌کنند.

از طرف کاربر: پس از ورود موفق، داشبورد/برنامه هفته خالی می‌ماند، هیچ داده‌ای بارگذاری نمی‌شود، و پیام «نشست منقضی شده» دیده می‌شود.

**راه‌حل پیشنهادی:**
فایل `src/proxy.ts` را به `src/middleware.ts` برگردانید و نام تابع `proxy` را به `middleware` تغییر دهید. فقط همین یک تغییر، اکثر قابلیت‌های اپ را دوباره فعال می‌کند. (توجه: بعد از این تغییر، باگ‌های امنیتی دیگر مثل CSRF فعال می‌شوند، پس آن‌ها را هم ببندید.)

---

### C2. `.env` با `DATABASE_URL` اشتباه (SQLite) commit شده، در حالی که schema PostgreSQL است

- **فایل / خط:** `.env:1` + `prisma/schema.prisma:7-10`
- **دسته:** پیکربندی / داده (بحرانی)
- **تأیید شده با `git ls-files .env`.**

**چه خطایی می‌دهد (ساده):**
- فایل `.env` در گیت commit شده (با وجود اینکه `.gitignore` آن را نادیده می‌گیرد — چون اول commit شده، потомس ignore نمی‌شود).
- محتوای آن: `DATABASE_URL=file:/home/z/my-project/db/custom.db` که یک مسیر **SQLite** است و حتی به مسیر sandbox محلی اشاره می‌کند.
- اما `schema.prisma` اعلام کرده `provider = "postgresql"`.
- Prisma Client برای PostgreSQL تولید می‌شود ولی URL به SQLite اشاره می‌کند → هنگام اتصال خطای `Unknown datasource provider` یا خطای اتصال می‌دهد. کل دیتابیس از کار می‌افتد.
- ضمناً مسیر `/home/z/my-project/...` که مسیر اختصاصی sandbox است، در گیت عمومی شده (نشت اطلاعات و عدم قابل‌حمل بودن).

**راه‌حل پیشنهادی:**
- `git rm --cached .env` تا از گیت حذف شود ولی روی دیسک بماند.
- در محیط استقرار (Liara) `DATABASE_URL` واقعی PostgreSQL را از طریق متغیرهای محیطی پنل Liara تنظیم کنید.
- یک فایل `.env.example` برای مستندسازی متغیرهای لازم بسازید.

---

### C3. تنظیم نبودن `AUTH_SECRET` در production → crash کل API

- **فایل / خط:** `src/lib/auth.ts:6-10` و `src/lib/edge-auth.ts:4-8`
- **دسته:** پیکربندی / استقرار (بحرانی)

**چه خطایی می‌دهد (ساده):**
هر دو فایل این کد را دارند:
```ts
if (process.env.NODE_ENV === 'production' && !process.env.AUTH_SECRET) {
  throw new Error('AUTH_SECRET is required in production');
}
```
این `throw` در زمان **import** ماژول اجرا می‌شود. در محیط production (مثل Liara که `NODE_ENV=production` است)، اگر `AUTH_SECRET` تنظیم نشده باشد، اولین بار که هر مسیر API این فایل را import کند، کل پردازش با 500 crash می‌کند. چون `.env` فقط `DATABASE_URL` دارد و `AUTH_SECRET` وجود ندارد، در Liara کل سیستم لاگین/احراز هویت از کار می‌افتد.

**راه‌حل پیشنهادی:**
`AUTH_SECRET` را با یک رشته تصادفی قوی در متغیرهای محیطی Liara تنظیم کنید. در `.env.example` هم مستند کنید.

---

### C4. `liara:release` (migration + seed) هرگز روی Liara اجرا نمی‌شود → دیتابیس خالی

- **فایل / خط:** `package.json:15` (`liara:release`) و `liara.json:4-7`
- **دسته:** پیکربندی / استقرار (بحرانی)

**چه خطایی می‌دهد (ساده):**
- `package.json` اسکریپت `liara:release` دارد که `prisma migrate deploy` و سپس `db:seed` را اجرا می‌کند (یعنی جداول را می‌سازد و داده اولیه می‌ریزد).
- اما `liara.json` هیچ فیلد `release` یا `build.command` ندارد. Liara فقط `npm run build` و سپس `npm run start` را اجرا می‌کند.
- نتیجه: migration‌ها هرگز اجرا نمی‌شوند → در دیتابیس Liara **هیچ جدولی ساخته نمی‌شود**. هر درخواست API با خطای `relation "User" does not exist` یا مشابه آن 500 می‌دهد.

**راه‌حل پیشنهادی:**
در `liara.json` فیلد `release` اضافه کنید (اگر Liara پشتیبانی می‌کند) یا بعد از هر deploy دستی `npm run liara:release` را اجرا کنید. همچنین `"build": { "command": "npm run build", ... }` را صریح بنویسید تا `prisma generate` حتماً اجرا شود.

---

### C5. IDOR در ویرایش آزمون: مشاور می‌تواند هر دانش‌آموزی را به آزمون خود اضافه کند

- **فایل / خط:** `src/app/api/exams/[id]/route.ts:76-87`
- **دسته:** امنیت / کنترل دسترسی (بحرانی)
- **تأیید شده با بررسی دستی.**

**چه خطایی می‌دهد (ساده):**
- مسیر `POST /api/exams` (ساخت آزمون) درست بررسی می‌کند که همه `studentIds` به مشاور لاگین‌شده تعلق دارند (خط 182-196).
- اما مسیر `PATCH /api/exams/[id]` (ویرایش) این بررسی را **کلاً انجام نمی‌دهد**. فقط چک می‌کند که فراخوان، سازنده آزمون باشد، بعد بی‌قیدوشرط لیست شرکت‌کنندگان را با هر `studentId` که کلاینت بفرستد جایگزین می‌کند.
- یک مشاور می‌تواند شناسه **هر دانش‌آموزی** (حتی دانش‌آموزان مشاور دیگر، یا شناسه‌های ناموجود) را به آزمون خودش اضافه کند.
- آن دانش‌آموزان بعداً آزمون را در داشبورد خود می‌بینند (عنوان، درس، تاریخ و نتیجه احتمالی برایشان لو می‌رود).

**راه‌حل پیشنهادی:**
همان بررسی `assignedAdvisorId === ctx.userId` (یا عضویت در مؤسسه) که در POST انجام می‌شود را در PATCH هم روی لیست جدید `studentIds` اعمال کنید، قبل از جایگزینی شرکت‌کنندگان.

---

### C6. اسکریپت `seed-from-excel.js` کل جدول Task را پاک می‌کند

- **فایل / خط:** `scripts/seed-from-excel.js:226`
- **دسته:** یکپارچگی داده / از دست رفتن داده (بحرانی)
- **تأیید شده با بررسی دستی.**

**چه خطایی می‌دهد (ساده):**
خط 226 این را اجرا می‌کند:
```js
const deletedTasks = await prisma.task.deleteMany({});
```
این دستور با `where` خالی، **تمام** ردیف‌های جدول `Task` (همه دانش‌آموزان، تمام تاریخچه، تمام منبع داده‌های آنالیز) را حذف می‌کند، قبل از اینکه دوباره دروس/فصل‌ها را بازسازی کند. اگر این اسکریپت روی دیتابیس production اجرا شود، تمام تاریخچه مطالعه کاربران نابود می‌شود. ضمناً حذف و بازسازی در یک تراکنش نیستند، پس اگر اسکریپت وسط کار قطع شود، دیتابیس خالی می‌ماند.

**راه‌حل پیشنهادی:**
- دستور `task.deleteMany` را کلاً حذف کنید (تسک‌ها از طریق `onDelete: SetNull` روی chapter/topic به‌طور خودکار FK خود را null می‌کنند — این رفتار درست است).
- یا اگر واقعاً نیاز به پاکسازی دارید، حذف را فقط محدود به تسک‌های مربوط به دروس بازسازی‌شده کنید و داخل یک تراکنش با بازسازی قرار دهید.

---

### C7. مقایسه امضای HMAC غیر زمان‌مند (timing attack)

- **فایل / خط:** `src/lib/auth.ts:57` و `src/lib/edge-auth.ts:42`
- **دسته:** امنیت (بحرانی)

**چه خطایی می‌دهد (ساده):**
توابع `verifyToken` و `verifyTokenEdge` امضای دریافتی را با `signature !== expectedSignature` (مقایسه رشته‌ای معمولی) مقایسه می‌کنند. این مقایسه در اولین بایتی که تفاوت دارد متوقف می‌شود و زمان اجرا به ميزان تطابق بستگی دارد. یک مهاجم تئوری می‌تواند با اندازه‌گیری زمان پاسخ، امضا را بایت‌به‌بایت حدس بزند (timing attack). در شبکه سخت است ولی الگوی ضعیف شناخته‌شده‌ای است. چون فقط امضا از جعل توکن جلوگیری می‌کند، این نقطه‌ضعف مهم است.

**راه‌حل پیشنهادی:**
در `auth.ts` از `crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expectedSignature))` استفاده کنید (بعد از بررسی هم‌طولی). در `edge-auth.ts` یک مقایسه زمان‌مند پیاده کنید (یا پولی‌فیل).

---

### C8. `requireAuth` به header قابل‌جعل توسط کلاینت اعتماد می‌کند

- **فایل / خط:** `src/lib/api-auth.ts:29-31` (با `src/proxy.ts:48`)
- **دسته:** امنیت / احراز هویت (بحرانی)

**چه خطایی می‌دهد (ساده):**
`getUserIdFromRequest` فقط `request.headers.get('x-user-id')` را می‌خواند و آن را به‌عنوان کاربر احرازشده قبول می‌کند. طراحی این است که middleware این header را با مقدار استخراج‌شده از کوکی امضاشده بازنویسی کند. اما:
- (الف) چون middleware فعلاً اجرا نمی‌شود (باگ C1)، این header هرگز ست نمی‌شود و همه 401 می‌گیرند.
- (ب) حتی اگر middleware برگردد، اگر درخواستی از مسیری غیر از matcher عبور کند (مثلاً با تنظیمات `next.config` اشتباه یا فراخوانی داخلی)، مهاجم می‌تواند خودش header `x-user-id` را بفرستد و خود را به‌عنوان هر کاربری جا بزند. هیچ‌گونه دفاع در عمق (defense-in-depth) وجود ندارد.

**راه‌حل پیشنهادی:**
یا توکن را داخل خود `requireAuth` دوباره تأیید کنید (به header اعتماد نکنید)، یا یک header مخفی داخلی اضافه کنید که middleware ست می‌کند و `requireAuth` آن را چک می‌کند تا یک `x-user-id` تنها از سوی کلاینت کافی نباشد.

---

### C9. جهش مستقیم state در هنگام drag-and-drop مرتب‌سازی تسک‌ها

- **فایل / خط:** `src/components/plan/SortableTaskList.tsx:122-127`
- **دسته:** React / جهش state (بحرانی)

**چه خطایی می‌دهد (ساده):**
```ts
const reordered = arrayMove(tasks, oldIndex, newIndex);
reordered.forEach((t, i) => { t.order = i; });  // ← مستقیماً آبجکت‌های داخل store را تغییر می‌دهد
onReorder(reordered);
```
`arrayMove` یک آرایه جدید برمی‌گرداند ولی **آبجکت‌های داخل آن همان رفرنس‌های store هستند**. پس `t.order = i` مستقیماً state Zustand را بدون `set()` تغییر می‌دهد. این باعث می‌شود React ممکن است re-render را رد کند (چون رفرنس آبجکت تغییر نکرده)، و UI با ترتیب واقعی هماهنگ نباشد. همچنین selector‌های memo شده روی `tasks` را خراب می‌کند.

**راه‌حل پیشنهادی:**
به جای جهش، آبجکت‌های جدید بسازید: `arrayMove(...).map((t, i) => ({ ...t, order: i }))`.

---

### C10. فراخوانی `useAppStore.getState()` داخل render

- **فایل / خط:** `src/components/plan/PlanView.tsx:566`
- **دسته:** React / خواندن غیرری‌اکتیو store (بحرانی)

**چه خطایی می‌دهد (ساده):**
```tsx
<TaskDetailsDialog ... grade={useAppStore.getState().user?.grade ?? 'دوازدهم'} ... />
```
`getState()` ری‌اکتیو نیست — اگر کاربر پایه/رشته خود را در تنظیمات تغییر دهد، این دیالوگ **پایه قدیمی** را نشان می‌دهد. ضمناً `detailsTaskId!` (assertion غیرnull) اگر ID وسط render null شود، کرش می‌کند.

**راه‌حل پیشنهادی:**
در بالای کامپوننت با `useAppStore((s) => s.user?.grade)` مشترک شوید و قبل از `onSave` با `if (!detailsTaskId) return` محافظت کنید.

---

### C11. AppShell با state احراز هویت قدیمی mount می‌شود (نشست منقضی‌شده)

- **فایل / خط:** `src/app/page.tsx:36-107`
- **دسته:** React / state قدیمی (بحرانی)

**چه خطایی می‌دهد (ساده):**
وقتی `localStorage` یک `onboardingComplete=true` قدیمی (از نشست قبلی که منقضی شده) دارد:
1. `fetch('/api/auth/me')` با 401 برمی‌گردد.
2. `clearAuthStorage()` فقط `localStorage` را پاک می‌کند، **نه store Zustand را**.
3. Store همچنان `onboardingComplete=true` و `userRole='STUDENT'` دارد (که هنگام ساخت store hydrate شده).
4. پس `authValidated=true` می‌شود، `isLoggedIn=true` ارزیابی می‌شود، و **AppShell + Dashboard کوتاهی با داده قدیمی render می‌شوند** قبل از اینکه اولین فراخوانی واقعی API 401 بدهد و کاربر را به لاگین پرت کند.
- کامنت خط 38-48 صراحتاً ادعا می‌کند این اتفاق نمی‌افتد — **کامنت اشتباه است**.

**راه‌حل پیشنهادی:**
در صورت 401 از `/api/auth/me`، علاوه بر `clearAuthStorage()`، store را هم ریست کنید: `setOnboardingComplete(false)`، `setUserRole(undefined)`، `setCurrentView('landing')`.

---

### C12. خطر حلقه بی‌نهایت در افکت «انتقال تسک‌های معوق»

- **فایل / خط:** `src/components/plan/PlanView.tsx:130-141`
- **دسته:** React / حلقه بی‌نهایت (بحرانی)

**چه خطایی می‌دهد (ساده):**
افکت تسک‌های گذشته با وضعیت PENDING را پیمایش می‌کند و برای هر کدام `updateTask(...)` را (بدون await) صدا می‌زند. `updateTask` به‌صورت optimistic است: ابتدا status را به `INCOMPLETE` تغییر می‌دهد، که باعث تغییر `tasks` → اجرای دوباره افکت می‌شود (خوب است، چون INCOMPLETE را رد می‌کند). **اما** اگر فراخوانی API شکست بخورد، `updateTask` تسک را به وضعیت PENDING قبلی برمی‌گرداند → `tasks` دوباره تغییر می‌کند → افکت دوباره اجرا → PENDING را می‌بیند → دوباره `updateTask` → دوباره شکست → **حلقه بی‌نهایت** فراخوانی API و churn در store. هر تکرار همچنین N فراخوانی موازی غیرbatch شده ایجاد می‌کند.

**راه‌حل پیشنهادی:**
IDهای تسک‌های انتقال‌یافته را در یک ref ذخیره کنید تا دوباره پردازش نشوند، و/یا انتقال را در try/catch بپیچید و تسک را به‌عنوان «انتقال ناموفق» علامت بزنید تا دوباره امتحان نشود. بهتر است همه به‌روزرسانی‌ها را در یک batch بفرستید.

---

### C13. `seed-history.js` تسک‌ها را با status غلط می‌سازد

- **فایل / خط:** `prisma/seed-history.js:85-105`
- **دسته:** یکپارچگی داده (بحرانی)

**چه خطایی می‌دهد (ساده):**
اسکریپت `completed: true/false/null` را ست می‌کند ولی هرگز `status` را ست نمی‌کند. مقدار پیش‌فرض schema برای `status` برابر `PENDING` است. پس هر تسک «تکمیل‌شده» تاریخی با `completed=true` **و** `status='PENDING'` ساخته می‌شود — که متناقض است. migration `20260809155000_add_task_status` این منطق را برای ردیف‌های قبلی backfill کرده، ولی ردیف‌های جدید این اسکریپت از آن عبور می‌کنند. نتیجه: `computeKpiTotals` (که بر اساس `status === 'COMPLETED'` فیلتر می‌کند) همه این تسک‌های «تکمیل‌شده» را نادیده می‌گیرد و آنالیز دانش‌آموز seed‌شده صفر ساعت نشان می‌دهد.

**راه‌حل پیشنهادی:**
`status` را صریح بر اساس `completed` ست کنید: `completed === true ? 'COMPLETED' : completed === false ? 'SKIPPED' : 'PENDING'`، یا از تابع `legacyTaskStatus()` در `task-status.ts` استفاده کنید.

---

# بخش ۲ — باگ‌های با اهمیت بالا (HIGH)

این باگ‌ها باعث رفتار اشتباه، نشت داده، یا مشکلات جدی کاربری می‌شوند ولی کل سیستم را از کار نمی‌اندازند.

---

### H1. CSRF: کوکی `SameSite=None` بدون محافظت CSRF روی مسیرهای تغییردهنده

- **فایل / خط:** `src/lib/auth.ts:159-165` (تنظیمات کوکی) + تمام مسیرهای POST/PATCH/DELETE
- **دسته:** امنیت / CSRF

**چه خطایی می‌دهد (ساده):**
کوکی نشست برای همه درخواست‌های غیرlocalhost با `SameSite=None; Secure` ست می‌شود (برای پشتیبانی از iframe cross-site). هیچ مسیری header `Origin`/`Referer` را چک نمی‌کند و توکن CSRF وجود ندارد. چون Next.js `request.json()` را فارغ از `Content-Type` پارس می‌کند، یک صفحه مهاجم می‌تواند با `fetch(..., {mode:'no-cors'})` یک بدنه JSON معتبر بفرستد، مرورگر کوکی `reval-session` را cross-site می‌فرستد، و مسیر تغییر را اجرا می‌کند (ساخت تسک، ارسال پیام broadcast، حذف آزمون، ...). مهاجم نمی‌تواند پاسخ را بخواند ولی اثر جانبی اجرا می‌شود. *(فعلاً به‌خاطر باگ C1 پنهان است — وقتی middleware برگردد، زنده می‌شود.)*

**راه‌حل پیشنهادی:**
یا یک توکن CSRF (double-submit) اضافه کنید، یا در هر مسیر mutating یک allowlist `Origin`/`Referer` چک کنید، یا به `SameSite=Lax` برگردید و برای iframe یک مسیر جداگانه در نظر بگیرید.

---

### H2. کد OTP در پاسخ HTTP برگردانده می‌شود وقتی `SMS_IR_MODE=sandbox`

- **فایل / خط:** `src/app/api/auth/otp/request/route.ts:14`
- **دسته:** امنیت / دور زدن احراز هویت

**چه خطایی می‌دهد (ساده):**
`requestOtp()` کد ۶ رقمی را برمی‌گرداند و مسیر آن را وقتی `isSmsSandbox()` درست باشد (یعنی `SMS_IR_MODE === 'sandbox'`) به‌عنوان `testCode` در بدنه JSON می‌فرستد. اگر sandbox به‌اشتباه در محیط مستقر روشن بماند، هر کسی می‌تواند برای هر شماره‌ای OTP بخواهد و بلافاصله کد را از پاسخ بخواند — **دور زدن کامل احراز هویت** برای ورود و ثبت‌نام.

**راه‌حل پیشنهادی:**
`testCode` را فقط وقتی `NODE_ENV !== 'production'` است برگردانید (یا کلاً حذف کنید و به لاگ سرور در dev تکیه کنید).

---

### H3. ویرایش آزمون آرایه خالی `studentIds: []` را نادیده می‌گیرد — پاک کردن شرکت‌کنندگان ممکن نیست

- **فایل / خط:** `src/app/api/exams/[id]/route.ts:82`
- **دسته:** منطق

**چه خطایی می‌دهد (ساده):**
شرط `if (newStudentIds && newStudentIds.length > 0)` است. وقتی کلاینت `studentIds: []` (برای حذف همه شرکت‌کنندگان) می‌فرستد، این شاخه رد می‌شود و شرکت‌کنندگان قدیمی باقی می‌مانند. پاسخ، آزمون را با لیست قدیمی برمی‌گرداند، پس کلاینت فکر می‌کند پاکسازی موفق بوده ولی دیتابیس همچنان ردیف‌های قدیمی را دارد.

**راه‌حل پیشنهادی:**
شرط را به `if (newStudentIds !== null)` (یا `if (Array.isArray(body.studentIds))`) تغییر دهید تا آرایه خالی صریح هم مسیر delete+recreate را طی کند.

---

### H4. عدم اعتبارسنجی `status` در ساخت/ویرایش آزمون — هر رشته‌ای ذخیره می‌شود

- **فایل / خط:** `src/app/api/exams/route.ts:130` و `src/app/api/exams/[id]/route.ts:73`
- **دسته:** mass assignment / اعتبارسنجی

**چه خطایی می‌دهد (ساده):**
`status = String(body.status ?? 'upcoming')` هر رشته‌ای را می‌پذیرد. ستون DB از نوع `String` آزاد است (نه enum واقعی)، پس مقادیری مثل `"hacked"` یا `"completed"` (قبل از وجود نتیجه) ذخیره می‌شوند. کد downstream که روی `status` سوییچ می‌کند (مثلاً results PUT که `'completed'` را خودکار ست می‌کند) توسط مقادیر خارجی گیج می‌شود.

**راه‌حل پیشنهادی:**
`status` را به `['upcoming','in-progress','completed']` محدود کنید و در غیر این صورت 400 برگردانید.

---

### H5. حذف کاربر (`users/[userId]` DELETE) دانش‌آموزان، مؤسسات و درخواست‌های اتصال را یتیم می‌کند

- **فایل / خط:** `src/app/api/users/[userId]/route.ts:27-31`
- **دسته:** یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
حذف نرم (soft-delete) فقط `isActive=false` و `deletedAt=now` را ست می‌کند. اگر کاربر حذف‌شده یک **ADVISOR** باشد، هر دانش‌آموز با `assignedAdvisorId = userId` به یک مشاور مرده اشاره می‌کند (دیگر نمی‌توانند تسک بسازند، و چون POST connection-request به `student.assignedAdvisorId` نگاه می‌کند و 409 می‌دهد، نمی‌توانند مشاور جدید بخواهند). اگر کاربر **INSTITUTE_MANAGER** باشد، `Institute.managerId` همچنان به کاربر حذف‌شده اشاره می‌کند. درخواست‌های اتصال معلق هم معلق می‌مانند.

**راه‌حل پیشنهادی:**
در همان تراکنش، `assignedAdvisorId` دانش‌آموزان را null کنید، `Institute.managerId` را مدیریت کنید (یا null یا cascade)، و درخواست‌های اتصال معلق را لغو/پایان دهید.

---

### H6. مدیر کل می‌تواند حساب خودش را حذف کند → قفل کامل سیستم

- **فایل / خط:** `src/app/api/users/[userId]/route.ts:23-31`
- **دسته:** منطق

**چه خطایی می‌دهد (ساده):**
هیچ محافظ `ctx.userId === userId` وجود ندارد. یک مدیر کل می‌تواند خودش را DELETE کند؛ `isActive` false می‌شود، پس فراخوانی بعدی `requireAuth` با 403 («حساب شما غیرفعال شده است») برمی‌گردد. اگر این تنها مدیر کل باشد، **سیستم کاملاً قفل می‌شود** و فقط bootstrap از طریق env می‌تواند آن را بازیابی کند.

**راه‌حل پیشنهادی:**
`if (ctx.userId === userId) return 400` اضافه کنید.

---

### H7. شرایط مسابقه: دو ACCEPT همزمان روی درخواست‌های اتصال یک دانش‌آموز

- **فایل / خط:** `src/app/api/connection-requests/[id]/route.ts:29-40`
- **دسته:** شرایط مسابقه / یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
یک دانش‌آموز می‌تواند همزمان چند درخواست PENDING به مشاوران مختلف داشته باشد. دو مشاور می‌توانند همزمان PATCH-ACCEPT کنند. داخل تراکنش، هرکدام `findUnique(student)` → چک `assignedAdvisorId` → `update assignedAdvisorId` انجام می‌دهد. با isolation پیش‌فرض READ COMMITTED، هر دو `null` می‌خوانند، هر دو چک را رد می‌کنند، هر دو می‌نویسند — آخرین نوشته برنده می‌شود. درخواست مشاور بازنده در وضعیت `ACCEPTED` می‌ماند ولی دانش‌آموز واقعاً به مشاور دیگر اختصاص یافته است. UI وضعیت ناسازگار نشان می‌دهد.

**راه‌حل پیشنهادی:**
از یک `UPDATE` اتمیک استفاده کنید: `db.user.updateMany({ where: { id: studentId, assignedAdvisorId: null }, data: { assignedAdvisorId: advisorId } })` و `result.count === 1` را چک کنید، یا یک unique partial index روی `assignedAdvisorId WHERE NOT NULL` اضافه کنید.

---

### H8. عدم صفحه‌بندی در `GET /api/tasks` — `findMany` نامحدود

- **فایل / خط:** `src/app/api/tasks/route.ts:24`
- **دسته:** عملکرد / احتمال timeout

**چه خطایی می‌دهد (ساده):**
`db.task.findMany({ where, orderBy: { order: 'asc' } })` هیچ `take`/`skip` ندارد. اگر `date` حذف شود، هر تسکی که دانش‌آموز تا حالا ساخته بارگذاری می‌شود. برای دانش‌آموزی با ماه‌ها/سال‌ها تاریخچه، این می‌تواند هزاران ردیف باشد و باعث پاسخ کند یا OOM شود.

**راه‌حل پیشنهادی:**
پیش‌فرض یک `take` معقول (مثلاً 200) با صفحه‌بندی cursor/offset؛ اگر بیشتر لازم است، بازه تاریخ اجباری شود.

---

### H9. مؤسسه POST: بدون try/catch، بدون اعتبارسنجی `subscriptionPlan`/`status`

- **فایل / خط:** `src/app/api/admin/institutes/route.ts:22-29`
- **دسته:** اعتبارسنجی + مدیریت خطا

**چه خطایی می‌دهد (ساده):**
`subscriptionPlan: body.subscriptionPlan || 'basic'` و `status: body.status || 'trial'` هر رشته truthy کلاینت را می‌پذیرند (بدون allowlist). اگر `createPublicCode` تصادفاً تصادم کند، یا یک تلفن تکراری از بررسی check-then-create TOCTOU عبور کند، خطای Prisma catch نمی‌شود (بدون try/catch) و به‌صورت یک 500 خام با بدنه JSON‌نشده نمایان می‌شود. کلاینت یک خطای شبکه عمومی می‌بیند.

**راه‌حل پیشنهادی:**
`subscriptionPlan` را به `['free','basic','pro','enterprise']` و `status` را به `['active','suspended','trial']` محدود کنید؛ در try/catch بپیچید و در صورت خطای unique-constraint 409 برگردانید.

---

### H10. mass assignment: `instituteId` بدون اعتبارسنجی در users POST و PATCH

- **فایل / خط:** `src/app/api/users/route.ts:51` و `src/app/api/users/[userId]/route.ts:13`
- **دسته:** mass assignment / یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
POST: `instituteId: body.instituteId || null` — هر رشته‌ای نوشته می‌شود. PATCH: `if ('instituteId' in body) data.instituteId = body.instituteId || null` — یک مدیر کل می‌تواند `instituteId` یک مدیر مؤسسه را null کند و `Institute.managerId` را به کاربری اشاره دهد که دیگر عضو نیست. همچنین یک مدیر کل می‌تواند یک STUDENT را بدون عبور از onboarding مؤسسه به مؤسسه‌ای تخصیص دهد.

**راه‌حل پیشنهادی:**
قبل از نوشتن، وجود و فعال بودن مؤسسه را تأیید کنید؛ در PATCH هم سازگاری role/institute را بررسی کنید.

---

### H11. streak قبل از تأیید ذخیره تسک افزایش می‌یابد؛ در صورت شکست rollback نمی‌شود

- **فایل / خط:** `src/components/dashboard/Dashboard.tsx:256-262` و `287-293`
- **دسته:** به‌روزرسانی optimistic نادرست / عدم rollback

**چه خطایی می‌دهد (ساده):**
`updateTask(...)` (بدون await) صدا زده می‌شود، بعد فوراً `incrementStreak()` و جشن اجرا می‌شوند، قبل از اینکه سرور تأیید کند. اگر سرور به‌روزرسانی را رد کند (مثلاً 500، شبکه)، `updateTask` تسک را برمی‌گرداند ولی streak قبلاً افزایش یافته و در localStorage ذخیره شده. streak کاربر برای تسکی که واقعاً ذخیره نشده بالا می‌رود.

**راه‌حل پیشنهادی:**
اول `await updateTask(...)`، بعد فقط در صورت موفقیت `incrementStreak()` و `celebrate()` را صدا بزنید.

---

### H12. جادوگر onboarding نقش را به STUDENT هاردکد کرده — انتخاب نقش کد مرده است

- **فایل / خط:** `src/components/onboarding/OnboardingWizard.tsx:509-517`
- **دسته:** کد مرده / قابلیت شکسته

**چه خطایی می‌دهد (ساده):**
افکت وقتی OTP ۶ رقمی وارد شد، `setRole('STUDENT')` را صدا می‌زند — همیشه STUDENT. کامپوننت‌های `StepRole` (خط 193-263) و `StepInstitute` (خط 411-457) تعریف شده‌اند ولی **هرگز render نمی‌شوند**. جادوگر فقط StepPhone → StepIdentity → StepAcademic را نشان می‌دهد. ثبت‌نام خودکار ADVISOR و INSTITUTE_MANAGER از طریق UI غیرممکن است، با وجود اینکه `ROLE_OPTIONS` و نوع `RegisterRole` نشان می‌دهند باید کار کند.

**راه‌حل پیشنهادی:**
یا `StepRole` را بین مرحله ۱ و ۲ render کنید و مرحله ۳ را بر اساس نقش شاخه کنید، یا کد مرده و export‌های فریبنده را حذف کنید.

---

### H13. CSS نامعتبر از ترکیب متغیر CSS + hex-alpha

- **فایل / خط:** `src/components/shared/NotificationCenter.tsx:81`
- **دسته:** CSS / استایل

**چه خطایی می‌دهد (ساده):**
```tsx
style={{ backgroundColor: `${colorValue}18` }}
```
که `colorValue = resolveColor(notification.color)` مثلاً `'var(--danger)'` برمی‌گرداند. نتیجه `var(--danger)18` است که **CSS نامعتبر** است — مرورگر آن را نادیده می‌گیرد، پس پس‌زمینه آیکن اعلان شفاف است. روی همه اعلان‌ها تأثیر می‌گذارد.

**راه‌حل پیشنهادی:**
از `color-mix(in srgb, var(--danger) 10%, transparent)` استفاده کنید، یا متغیر CSS را در زمان اجرا به hex تبدیل کنید و سپس alpha را اضافه کنید.

---

### H14. شنونده `visibilitychange` هرگز حذف نمی‌شود — نشت حافظه

- **فایل / خط:** `src/components/plan/StudySessionTimer.tsx:87-101`
- **دسته:** نشت حافظه

**چه خطایی می‌دهد (ساده):**
هر بار که `StudySessionTimer` mount/unmount می‌شود (که با هر render کارت تسک اتفاق می‌افتد)، یک شنونده `visibilitychange` ناشناس به `document` اضافه می‌شود و هرگز حذف نمی‌شود. در طول یک نشست ده‌ها شنونده جمع می‌شود که هر کدام `pauseAll()` را در مخفی شدن تب صدا می‌زنند — افت عملکرد و فراخوانی‌های تکراری pause.

**راه‌حل پیشنهادی:**
تابع ناشناس را به یک const نام‌دار تبدیل کنید و در cleanup `removeEventListener` کنید.

---

### H15. `updateTask` با هر فشردن کلید — شرایط مسابقه + عدم توانایی ست کردن 0

- **فایل / خط:** `src/components/plan/PlanView.tsx:584` و `594`
- **دسته:** شرایط مسابقه / فراخوانی API بیش از حد

**چه خطایی می‌دهد (ساده):**
تایپ «30» دو فراخوانی API می‌سازد: یکی برای «3»، یکی برای «30». فراخوانی دوم می‌تواند با اولی مسابقه کند؛ اگر سرور از ترتیب خارج پردازش کند، تسک مقدار اشتباه می‌گیرد. ضمناً `Number("") || null` به `null` ارزیابی می‌شود (خوب)، ولی `Number("0") || null` هم به `null` ارزیابی می‌شود — **نمی‌توانید مقدار را 0 کنید** (مثلاً ۰ تست).

**راه‌حل پیشنهادی:**
ورودی را debounce کنید (مثلاً 400ms) قبل از `updateTask`؛ از `e.target.value === '' ? null : Number(e.target.value)` برای اجازه دادن به 0 استفاده کنید.

---

### H16. `setTimeout` در CelebrationOverlay پاک نمی‌شود

- **فایل / خط:** `src/components/shared/CelebrationOverlay.tsx:113-115`
- **دسته:** نشت حافظه / به‌روزرسانی state پس از unmount

**چه خطایی می‌دهد (ساده):**
تایم‌اوت در callback `subscribe` ساخته می‌شود ولی هرگز ذخیره یا پاک نمی‌شود. اگر کامپوننت قبل از 3.8 ثانیه unmount شود، `setParticles([])` روی کامپوننت unmount شده اجرا می‌شود. همچنین جشن‌های سریع چندین تایم‌اوت انباشته می‌کنند.

**راه‌حل پیشنهادی:**
ID تایم‌اوت را در یک ref ذخیره و در cleanup افکت پاک کنید.

---

### H17. کد OTP تستی در toast‌های production نمایش داده می‌شود

- **فایل / خط:** `src/components/auth/LoginPage.tsx:113` و `src/components/onboarding/OnboardingWizard.tsx:531`
- **دسته:** امنیت / نشت اطلاعات

**چه خطایی می‌دهد (ساده):**
اگر backend `testCode` برگرداند (حالت dev)، OTP در یک toast نمایش داده می‌شود که هر کسی که به صفحه نگاه کند می‌بیند. در production نباید اتفاق بیفتد، ولی اگر flag به‌اشتباه روشن بماند، هر بازدیدکننده‌ای می‌تواند OTP را از toast بخواند.

**راه‌حل پیشنهادی:**
با `process.env.NODE_ENV === 'development'` گیت کنید یا کلاً حذف کنید.

---

### H18. `queueMicrotask` در بارگذاری ماژول می‌تواند 401 زودرس بفرستد

- **فایل / خط:** `src/lib/store.ts:1446-1456`
- **دسته:** شرایط مسابقه / فراخوانی API زودرس

**چه خطایی می‌دهد (ساده):**
در بارگذاری ماژول، اگر `localStorage` یک `onboardingComplete=true` قدیمی داشته باشد، این microtask `refreshNotifications()` → `loadInboxMessages()` → `/api/messages` → 401 می‌سازد. 401 مقدار `unauthTriggered = true` را در `api-client.ts` ست می‌کند. هرچند `registerUnauthHandler` (توسط SessionGuard هنگام mount) این flag را ریست می‌کند، یک پنجره بین microtask و mount SessionGuard وجود دارد که flag با handler null درست است. اگر در آن پنجره مسیر دیگری 401 بدهد، بی‌صدا بلعیده می‌شود.

**راه‌حل پیشنهادی:**
رفرش اعلان‌ها را به افکت mount AppShell منتقل کنید (بعد از `authValidated=true`).

---

### H19. `seed.ts` برای تسک و آزمون idempotent نیست (در اجرای مجدد تکرار می‌سازد)

- **فایل / خط:** `prisma/seed.ts:267-289` (تسک)، `292-335` (آزمون)، `349-362` (نتایج)
- **دسته:** یکپارچگی داده / seed غیرidempotent

**چه خطایی می‌دهد (ساده):**
کاربران، مؤسسه و دروس از `upsert` (idempotent) استفاده می‌کنند. ولی تسک‌ها از `prisma.task.create`، آزمون‌ها از `prisma.exam.create`، و ExamParticipants/ExamResults از `create` استفاده می‌کنند. اجرای دو بار `bun run db:seed` ۲× تسک، ۲× آزمون، ۲× شرکت‌کننده می‌سازد (که برای شرکت‌کنندگان در اجرای دوم `@@unique([examId, studentId])` را نقض می‌کند و seed را crash می‌دهد).

**راه‌حل پیشنهادی:**
از `upsert` با کلید قطعی استفاده کنید (مثلاً برای تسک `studentId + date + order` — نیاز به محدودیت unique دارد؛ یا `deleteMany` تسک‌های seed قدیمی با یک نشانگر). برای آزمون، upsert بر اساس عنوان قطعی.

---

### H20. `seed-history.js` از cuid دروس هاردکد شده استفاده می‌کند که در دیتابیس تازه وجود ندارند

- **فایل / خط:** `prisma/seed-history.js:15-19`
- **دسته:** یکپارچگی داده / ID هاردکد شده

**چه خطایی می‌دهد (ساده):**
آرایه `SUBJECTS` پنج cuid خاص را هاردکد کرده (`cmsk1ycki000mp0c3ga04fwju` و ...). این IDها فقط در دیتابیس خاصی که قبلاً تولید شده‌اند معتبرند. در هر دیتابیس دیگری (یا بعد از `prisma migrate reset`)، `prisma.task.create` با این `subjectId`‌ها با خطای FK مواجه می‌شود و اسکریپت crash می‌کند.

**راه‌حل پیشنهادی:**
دروس را با `normalizedName` جستجو کنید (`findUnique({ where: { normalizedName } })`) به جای ID هاردکد.

---

### H21. `publicCode` هم `@default(cuid())` دارد هم تولید دستی — شرایط مسابقه TOCTOU

- **فایل / خط:** `prisma/schema.prisma:26`، `src/lib/public-code.ts:6-14`، `src/app/api/auth/login/route.ts:56`، `register/route.ts:107`
- **دسته:** شرایط مسابقه / احتمال نقض unique

**چه خطایی می‌دهد (ساده):**
`createPublicCode` یک `findUnique` برای چک تصادم می‌کند، بعد کد را برمی‌گرداند؛ فراخواننده بعداً آن را به `user.create` می‌فرستد. بین چک و create، یک درخواست همزمان می‌تواند کاربری با همان کد بسازد. محدودیت unique دیتابیس این را می‌گیرد، ولی خطا به‌صورت یک 500 عمومی («خطای سرور») نمایان می‌شود به جای retry. با ۶ کاراکتر از الفبای ۳۲تایی (~10⁹ فضای جستجو) تصادم نادر است ولی شرایط مسابقه زیر بار واقعی است. ضمناً `@default(cuid())` یعنی ردیف‌هایی بدون `publicCode` صریح، یک کد با فرمت cuid (۲۴ کاراکتر) می‌گیرند — دو فرمت نادرست در یک ستون.

**راه‌حل پیشنهادی:**
چک+create را در یک تراکنش بپیچید، یا در خطای `P2002` با کد جدید retry کنید. `@default(cuid())` را حذف کنید چون هر مسیر create حالا کد صریح می‌فرستد.

---

### H22. `seed-subjects.ts` در هر اجرا GradeSubject/TopicMode را حذف می‌کند و لینک‌های برنامه درسی تسک‌ها را یتیم می‌کند

- **فایل / خط:** `prisma/seed-subjects.ts:119-121`
- **دسته:** یکپارچگی داده / reseed مخرب

**چه خطایی می‌دهد (ساده):**
`seedSubjectTree` برای هر درس `gradeSubject.deleteMany` و `topicMode.deleteMany` صدا می‌زند. چون Chapter→Topic با GradeSubject cascade حذف می‌شوند، این همه فصل‌ها و گفتارها را هم پاک می‌کند. تسک‌های موجود که به آن `chapterId`/`topicId`/`topicModeId` اشاره دارند، FK خود را null می‌گیرند (طبق `onDelete: SetNull`) و لینک برنامه درسی به‌صورت بی‌صدا از تسک‌های واقعی کاربران حذف می‌شود. اسکریپت به‌عنوان «idempotent» مستند شده ولی واقعاً مخرب است.

**راه‌حل پیشنهادی:**
از `upsert` برای GradeSubject/Chapter/Topic/TopicMode استفاده کنید (مثل `sync-curriculum-from-csv.ts`)، یا حذف‌ها را پشت یک flag صریح `--reset` بگذارید.

---

### H23. `ExamResult.score` از نوع `Int?` است ولی API اعشار را می‌پذیرد → 500

- **فایل / خط:** `prisma/schema.prisma:210` و `src/app/api/exams/[id]/results/route.ts:93-100`
- **دسته:** خطای نوع / خطای DB مدیریت‌نشده

**چه خطایی می‌دهد (ساده):**
ستون Prisma `score Int?` است. مسیر فقط `typeof r.score !== 'number'` و `0 <= score <= totalScore` را چک می‌کند ولی `Number.isInteger` را نه. اگر کلاینت `score: 18.5` بفرستد (رایج برای نمرات ایرانی از ۲۰)، اعتبارسنجی رد می‌شود، بعد `tx.examResult.createMany` خطای نوع Prisma می‌اندازد، تراکنش سقط می‌کند، و کاربر یک 500 عمومی می‌بیند. نوع TS `ExamResult.score: number | null` هم اعشار را می‌پذیرد و frontend را گمراه می‌کند.

**راه‌حل پیشنهادی:**
`!Number.isInteger(r.score)` را به اعتبارسنجی اضافه کنید (400 با پیام «نمره باید عدد صحیح باشد») یا ستون را به `Float?`/`Decimal?` تغییر دهید اگر نمره اعشاری منظور است.

---

### H24. اعتبارسنجی `rank` به‌عنوان عدد صحیح انجام نمی‌شود

- **فایل / خط:** `src/app/api/exams/[id]/results/route.ts:101-106`
- **دسته:** خطای نوع / یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
پیام خطا می‌گوید «رتبه باید یک عدد صحیح مثبت باشد» ولی چک فقط `typeof r.rank !== 'number' || r.rank < 1` است — یک اعشاری مثل `1.5` رد می‌شود و در نوشتن DB شکست می‌خورد.

**راه‌حل پیشنهادی:**
`|| !Number.isInteger(r.rank)` را به شرط اضافه کنید.

---

### H25. PATCH آزمون ردیف‌های `ExamResult` را هنگام حذف شرکت‌کنندگان یتیم می‌کند

- **فایل / خط:** `src/app/api/exams/[id]/route.ts:82-87`
- **دسته:** یکپارچگی داده / رکوردهای یتیم

**چه خطایی می‌دهد (ساده):**
وقتی لیست شرکت‌کنندگان جایگزین می‌شود، `examParticipant.deleteMany` شرکت‌کنندگان قدیمی را حذف می‌کند (چیز بیشتری را cascade نمی‌کند)، ولی ردیف‌های `ExamResult` برای دانش‌آموزان حذف‌شده باقی می‌مانند. `ExamResult` فقط `onDelete: Cascade` از `Exam` دارد، نه از `ExamParticipant`. پس یک دانش‌آموز حذف‌شده از آزمون همچنان یک ردیف نتیجه دارد، و `GET /api/exams/[id]/results` نتایج دانش‌آموزانی که دیگر شرکت‌کننده نیستند برمی‌گرداند.

**راه‌حل پیشنهادی:**
بعد از `examParticipant.deleteMany`، ردیف‌های `examResult` برای studentId‌هایی که دیگر در لیست جدید نیستند هم حذف کنید (در همان تراکنش).

---

### H26. `GET /api/users` تمام تسک‌های همه کاربران را بارگذاری می‌کند (بدون فیلتر)

- **فایل / خط:** `src/app/api/users/route.ts:29-39`
- **دسته:** عملکرد / احتمال OOM

**چه خطایی می‌دهد (ساده):**
`userInclude` با ولع `tasks` را برای هر کاربر و `students.tasks` را برای هر مشاور بارگذاری می‌کند. روی دیتابیسی با هزاران کاربر و ماه‌ها تاریخچه تسک، این یک کوئری کل جدول `Task` (احتمالاً میلیون‌ها ردیف) را در حافظه بارگذاری می‌کند فقط برای محاسبه `completionRate` و `studyHours`. هیچ فیلتر تاریخ، هیچ محدودیت، هیچ صفحه‌بندی. به‌شدت افت می‌کند و می‌تواند OOM کند.

**راه‌حل پیشنهادی:**
از کوئری‌های aggregate (`_count`، `aggregate`) یا یک endpoint آماری جداگانه با فیلتر تاریخ استفاده کنید.

---

### H27. lockfile تکراری: `bun.lock` و `package-lock.json` هر دو tracked

- **فایل / خط:** `bun.lock` و `package-lock.json` (هر دو tracked)
- **دسته:** وابستگی / ناسازگاری build

**چه خطایی می‌دهد (ساده):**
هر دو lockfile در گیت tracked هستند. اسکریپت‌های `package.json` از `npm run` استفاده می‌کنند (Liara npm را می‌خواند)، ولی `.zscripts/dev.sh` از `bun install`/`bun run` استفاده می‌کند. اگر توسعه‌دهنده محلی از bun استفاده کند (که `bun.lock` را می‌خواند)، درخت وابستگی‌های حل‌شده می‌تواند با Liara (که `package-lock.json` را می‌خواند) متفاوت باشد → «در dev کار می‌کند، در Liara خراب می‌شود».

**راه‌حل پیشنهادی:**
یک package manager انتخاب کنید. اگر با npm روی Liara deploy می‌کنید، `bun.lock` را حذف و `git rm` کنید.

---

### H28. `bun run db:push` به اسکریپت تعریف‌نشده اشاره دارد

- **فایل / خط:** `.zscripts/dev.sh:128-131` و `.zscripts/database-runtime-build.sh:22-25`
- **دسته:** خطای اسکریپت / شکست build

**چه خطایی می‌دهد (ساده):**
هر دو اسکریپت `bun run db:push` صدا می‌زنند. `package.json` اسکریپت `db:generate`، `db:migrate`، `db:deploy`، `db:seed`، `db:setup`، `liara:release`، `db:reset` را تعریف کرده — ولی **`db:push` وجود ندارد**. `bun run db:push` با «Script 'db:push' not found» شکست می‌خورد. build sandbox در این مرحله شکست می‌خورد.

**راه‌حل پیشنهادی:**
یا `"db:push": "prisma db push"` را به `package.json` اضافه کنید، یا فراخوانی‌های `.zscripts` را به `bunx prisma db push` تغییر دهید.

---

### H29. `SMS_IR_API_KEY` و `SMS_IR_OTP_TEMPLATE_ID` تنظیم نشده‌اند → OTP در production خراب

- **فایل / خط:** `src/lib/sms-ir.ts:8-11` و `.env:1`
- **دسته:** استقرار / env

**چه خطایی می‌دهد (ساده):**
`sendSmsIrVerification()` اگر `SMS_IR_API_KEY` تنظیم نباشد یا `SMS_IR_OTP_TEMPLATE_ID` عدد صحیح معتبر نباشد، `SMS_IR_NOT_CONFIGURED` می‌اندازد. `.env` هیچ‌کدام را ندارد. جریان OTP (ثبت‌نام و ورود) به این تابع وابسته است. در Liara، اگر این envها ست نشده باشند، هیچ کاربری نمی‌تواند ثبت‌نام یا وارد شود — حتی ورود مدیر کل (که OTP لازم دارد) هم شکست می‌خورد.

**راه‌حل پیشنهادی:**
`SMS_IR_API_KEY`، `SMS_IR_OTP_TEMPLATE_ID` و اختیاری `SMS_IR_MODE` را در متغیرهای محیطی Liara تنظیم کنید.

---

### H30. مسیرهای `/home/z/my-project/...` هاردکد شده در فایل‌های commit شده

- **فایل / خط:**
  - `scripts/start-dev-daemon.py:15-17`
  - `scripts/seed-from-excel.js:21`
  - `scripts/keep-server-alive.sh:3`
  - `.zscripts/build.sh:13`
  - `.zscripts/mini-services-install.sh:4` و `.zscripts/mini-services-build.sh:4`
- **دسته:** اسکریپت / قابل‌حمل بودن استقرار

**چه خطایی می‌دهد (ساده):**
این اسکریپت‌ها مسیر sandbox Z-AI `/home/z/my-project` را هاردکد کرده‌اند. مخزن واقعی در `/home/z/reval-analysis` است (و در Liara مسیر container مثل `/app`). هر کدام از این اسکریپت‌ها خارج از sandbox با «directory not found» شکست می‌خورند. `keep-server-alive.sh` بلافاصله شکست می‌خورد چون `/home/z/my-project/reval` وجود ندارد.

**راه‌حل پیشنهادی:**
مسیرهای هاردکد را با `$(cd "$(dirname "$0")/.." && pwd)` یا `process.cwd()` یا env var جایگزین کنید.

---

### H31. `scripts/keep-server-alive.sh` — cd خراب + هش chunk هاردکد شده

- **فایل / خط:** `scripts/keep-server-alive.sh:3,15`
- **دسته:** خطای اسکریپت

**چه خطایی می‌دهد (ساده):**
خط ۳ `cd /home/z/my-project/reval` — این مسیر در مخزن وجود ندارد. اسکریپت بلافاصله با «no such file or directory» خارج می‌شود. خط ۱۵ `.next/standalone/.next/static/chunks/58c60a5fa6b544da.js` را چک می‌کند — یک نام فایل chunk با هش محتوا که در هر build تغییر می‌کند. بعد از اولین rebuild، این چک همیشه شکست می‌خورد و مرحله کپی static-asset به‌طور مکرر اجرا می‌شود.

**راه‌حل پیشنهادی:**
از مسیر نسبی یا `$(dirname "$0")/..` استفاده کنید. چک هش chunk را حذف و به جای آن وجود دایرکتوری `.next/standalone/.next/static/` را چک کنید.

---

### H32. `scripts/seed-from-excel.js` به فایل اکسل ناموجود اشاره دارد

- **فایل / خط:** `scripts/seed-from-excel.js:21`
- **دسته:** خطای اسکریپت

**چه خطایی می‌دهد (ساده):**
`EXCEL_PATH = '/home/z/my-project/upload/Title Reval.xlsx'`. دایرکتوری `upload/` در `.gitignore` است و در مخزن نیست. اجرای این اسکریپت همیشه با «file not found» شکست می‌خورد.

**راه‌حل پیشنهادی:**
یا اسکریپت را حذف کنید، یا مسیر را از طریق argv/env قابل پیکربندی کنید.

---

### H33. `.zscripts/dev.pid` در گیت tracked — artifact زمان اجرا قدیمی

- **فایل / خط:** `.zscripts/dev.pid` (محتوا: `998`)
- **دسته:** بهداشت گیت / استقرار

**چه خطایی می‌دهد (ساده):**
یک فایل PID از اجرای گذشته dev-daemon در مخزن commit شده. `.gitignore:70` `.zscripts/*.pid` دارد، ولی این فایل قبل از آن قانون اضافه شده، پس tracked باقی مانده. در Liara یا هر clone تازه، این فایل یک PID بی‌معنی `998` دارد که می‌تواند اسکریپت‌هایی که آن را می‌خوانند گیج کند.

**راه‌حل پیشنهادی:**
`git rm --cached .zscripts/dev.pid` و commit کنید.

---

# بخش ۳ — باگ‌های متوسط (MEDIUM)

این باگ‌ها کیفیت و قابلیت نگهداری را تحت تأثیر قرار می‌دهند ولی معمولاً باعث شکست کامل نمی‌شوند.

---

### M1. INSTITUTE_MANAGER با `instituteId: null` می‌تواند آزمون‌های `instituteId: null` را مدیریت کند

- **فایل / خط:** `src/app/api/exams/[id]/route.ts:43-45` (و `[id]/results/route.ts:50-52`، `189-191`)
- **دسته:** ارتقاء امتیاز edge case

**چه خطایی می‌دهد (ساده):**
`isManagerOfInstitute = ctx.user.role === 'INSTITUTE_MANAGER' && existing.instituteId === ctx.user.instituteId`. اگر هر دو `null` باشند (یک مدیر بدون `instituteId` ساخته شده، و یک آزمون با `instituteId: null`)، `null === null` درست است. مدیر misconfigured می‌تواند آن آزمون را PATCH/DELETE کند و نتایج آن را بخواند/بنویسد.

**راه‌حل پیشنهادی:**
وقتی `ctx.user.instituteId` null است، تطابق را رد کنید، یا در زمان ساخت کاربر تضمین کنید INSTITUTE_MANAGER همیشه institute دارد.

---

### M2. `tasks/batch` POST قبل از تراکنش N×۳ کوئری DB می‌زند

- **فایل / خط:** `src/app/api/tasks/batch/route.ts:50,98,105`
- **دسته:** N+1 / عملکرد

**چه خطایی می‌دهد (ساده):**
برای هر تسک در batch، حلقه به‌ترتیب `canCreateTaskForStudent` (۱ کوئری)، `getEligibleTaskSubject` (۱ کوئری)، و `validateTaskCurriculum` (تا ۳ کوئری موازی) را await می‌کند. یک batch ۵۰تایی = ۱۵۰+ رفت‌وبرگشت متوالی قبل از شروع تراکنش insert. import برنامه‌های AI بزرگ کند خواهد بود.

**راه‌حل پیشنهادی:**
lookupهای دانش‌آموز/درس/برنامه درسی را batch کنید (یک `findMany` با `in: [...]`)، یا دانش‌آموز را یک بار prefetch و reuse کنید.

---

### M3. `activityTypes` اعتبارسنجی آیتم‌ها به‌عنوان رشته انجام نمی‌شود

- **فایل / خط:** `src/app/api/tasks/route.ts:55` و `src/app/api/tasks/batch/route.ts:135`
- **دسته:** اعتبارسنجی / یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
تنها چک `Array.isArray(body.activityTypes)` است. آیتم‌ها می‌توانند آبجکت، عدد، null و ... باشند. آن‌ها `JSON.stringify` و ذخیره می‌شوند. کلاینت `string[]` انتظار دارد؛ آیتم‌های غیر رشته‌ای هنگام render chip‌های فعالیت UI را کرش می‌کنند.

**راه‌حل پیشنهادی:**
هر آیتم را با `typeof item === 'string' && item.length > 0` اعتبارسنجی کنید.

---

### M4. `users/[userId]` PATCH تمام تسک‌های کاربر را بارگذاری می‌کند (بدون فیلتر/صفحه‌بندی)

- **فایل / خط:** `src/app/api/users/[userId]/route.ts:16`
- **دسته:** N+1 / عملکرد

**چه خطایی می‌دهد (ساده):**
`include: { tasks: {...}, students: { ... tasks: ... } }` هر ردیف تسک برای کاربر (و برای هر دانش‌آموز اگر مشاور) را بارگذاری می‌کند. برای مشاوری با ۲۰۰ دانش‌آموز × ۱۰۰۰ تسک، ۲۰۰k ردیف در حافظه فقط برای محاسبه نرخ تکمیل بارگذاری می‌شود.

**راه‌حل پیشنهادی:**
از `aggregate`/`groupBy` برای محاسبه counts و sums سمت سرور استفاده کنید.

---

### M5. `students` GET تسک‌های هفتگی هر دانش‌آموز را با include رابطه نامحدود بارگذاری می‌کند

- **فایل / خط:** `src/app/api/students/route.ts:55-75`
- **دسته:** عملکرد

**چه خطایی می‌دهد (ساده):**
`findMany` با `select: { tasks: { where: { date: { gte, lte } } } }` — برای مشاوری با ۵۰۰ دانش‌آموز و هفته شلوغ (۵۰ تسک/دانش‌آموز)، ۲۵k ردیف تسک در یک پاسخ برمی‌گرداند. هیچ `take` روی کوئری بیرونی هم نیست.

**راه‌حل پیشنهادی:**
`take` روی `findMany` بیرونی اضافه کنید (صفحه‌بندی) و برای آمار هر دانش‌آموز از `aggregate` استفاده کنید.

---

### M6. `grades/[gradeSubjectId]` و `topic-modes/[modeId]` مالکیت نسبت به `subjectId` مسیر را تأیید نمی‌کنند

- **فایل / خط:** `src/app/api/subjects/[subjectId]/grades/[gradeSubjectId]/route.ts:8-53` و `src/app/api/subjects/[subjectId]/topic-modes/[modeId]/route.ts:6-54`
- **دسته:** IDOR (یکپارچگی داده، فقط مدیر کل)

**چه خطایی می‌دهد (ساده):**
پارامتر مسیر `subjectId` استخراج می‌شود ولی هرگز استفاده نمی‌شود. یک مدیر کل می‌تواند یک `GradeSubject`/`TopicMode` متعلق به درس B را با فراخوانی `/api/subjects/subjectA/grades/gradeSubjectOfB` تغییر دهد. `tx.task.updateMany({ where: { topicModeId: modeId } })` بعداً `topic` را روی تسک‌هایی که ممکن است به درس دیگری تعلق داشته باشند بازنویسی می‌کند.

**راه‌حل پیشنهادی:**
`GradeSubject`/`TopicMode` را fetch و `subjectId` آن را با `subjectId` مسیر مقایسه کنید، در غیر این صورت 404.

---

### M7. عدم اعتبارسنجی فرمت تاریخ در ساخت/ویرایش آزمون و تسک

- **فایل / خط:** `src/app/api/exams/route.ts:123`، `exams/[id]/route.ts:69`، `tasks/route.ts:33`، `tasks/[taskId]/route.ts:93`
- **دسته:** اعتبارسنجی / یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
`date` فقط به‌عنوان `typeof === 'string'` چک می‌شود. رشته‌های دلخواه مثل `"banana"` ذخیره می‌شوند. endpoint GET تسک‌ها با `date: { gte: startDate, lte: endDate }` فیلتر می‌کند — چون `date` ستون متنی است، مقایسه رشته‌ای انجام می‌شود و یک تاریخ malformed هرگز با هیچ بازه‌ای مطابقت نمی‌کند، و آن تسک را از تمام کوئری‌های فیلتر شده با تاریخ مخفی می‌کند. تسک هنوز وجود دارد ولی در UI نامرئی است.

**راه‌حل پیشنهادی:**
با یک regex ISO-date دقیق (`/^\d{4}-\d{2}-\d{2}$/`) اعتبارسنجی کنید یا با `new Date()` پارس و `isValid` چک کنید.

---

### M8. مشاوران نمی‌توانند نتایج آزمون دانش‌آموزان خود را ببینند اگر آزمون توسط شخص دیگری ساخته شده

- **فایل / خط:** `src/app/api/exams/[id]/results/route.ts:186-201`
- **دسته:** شکاف منطق احراز هویت

**چه خطایی می‌دهد (ساده):**
endpoint نتایج GET اجازه می‌دهد: سازنده، مدیر کل، مدیر مؤسسه، یا دانش‌آموز شرکت‌کننده. ولی مشاور اختصاص‌یافته یک دانش‌آموز شرکت‌کننده را **اجازه نمی‌دهد**. پس اگر یک مدیر مؤسسه (یا مشاور دیگر) آزمونی بسازد که شامل دانش‌آموز مشاور باشد، مشاور نمی‌تواند نمره/رتبه آن دانش‌آموز را ببیند. خود آزمون هم برای مشاور نامرئی است (GET `/api/exams` برای مشاور `createdById = ctx.userId` را فیلتر می‌کند).

**راه‌حل پیشنهادی:**
`|| (ctx.user.role === 'ADVISOR' && exam.participants.some(p => p.studentId به ctx.userId اختصاص یافته))` را به چک احراز هویت اضافه کنید.

---

### M9. `/api/auth/me` مقدار `instituteId` را برنمی‌گرداند

- **فایل / خط:** `src/app/api/auth/me/route.ts:24-40`
- **دسته:** داده مفقود / UX

**چه خطایی می‌دهد (ساده):**
`select` مقدار `instituteId` را حذف می‌کند. یک کلاینت INSTITUTE_MANAGER نمی‌تواند از `/me` به‌تنهایی تشخیص دهد کدام مؤسسه را مدیریت می‌کند، و به یک رفت‌وبرگشت اضافی نیاز دارد. بعد از رفرش صفحه که فقط `/me` صدا می‌زند، context مؤسسه گم می‌شود.

**راه‌حل پیشنهادی:**
`instituteId: true` را به `select` اضافه کنید.

---

### M10. Institute PATCH روی دیکشنری data خالی کرش می‌کند

- **فایل / خط:** `src/app/api/admin/institutes/[instituteId]/route.ts:7-11`
- **دسته:** مدیریت خطا مفقود / کرش زمان اجرا

**چه خطایی می‌دهد (ساده):**
اگر کلاینت بدنه‌ای بفرستد که هیچ‌کدام از `name`، `subscriptionPlan`، `status` با allowlist‌ها مطابقت ندارند، `data` به‌عنوان `{}` باقی می‌ماند. `db.institute.update({ where: {...}, data: {} })` خطای اعتبارسنجی Prisma می‌اندازد («Argument `data` is missing»). try/catch وجود ندارد، پس خطا به‌صورت 500 خام با بدنه JSON‌نشده نمایان می‌شود.

**راه‌حل پیشنهادی:**
قبل از update `if (Object.keys(data).length === 0) return 400` چک کنید و در try/catch بپیچید.

---

### M11. آزمون POST: `studentIds` برای SUPER_ADMIN تأیید وجود/نقش نمی‌شود

- **فایل / خط:** `src/app/api/exams/route.ts:182-212`
- **دسته:** یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
کامنت می‌گوید «برای مدیر کل چک را رد کنید تا انعطاف‌پذیر باشد». یک مدیر کل می‌تواند ID ناموجود (خطای FK → 500)، ID کاربران غیرفعال، یا ID کاربران با نقش ADVISOR/INSTITUTE_MANAGER بفرستد. مورد اخیر ردیف‌های `ExamParticipant` می‌سازد که آزمون را به یک غیردانش‌آموز لینک می‌کند و در لیست «دانش‌آموز» ظاهر می‌شود و UI نتایج را خراب می‌کند.

**راه‌حل پیشنهادی:**
حداقل `role: 'STUDENT'` را برای همه ID‌های ارسال‌شده تأیید کنید، فارغ از نقش فراخوان.

---

### M12. `User.role`، `Exam.status`، `ConnectionRequest.status`، `Task.fieldType` رشته آزاد هستند به جای enum واقعی

- **فایل / خط:** `prisma/schema.prisma:19,181,67,66,141`
- **دسته:** یکپارچگی داده / ناسازگاری schema

**چه خطایی می‌دهد (ساده):**
تنها `TaskStatus` یک enum واقعی PostgreSQL است. بقیه «enum‌مانند» ولی رشته آزاد با default هستند. کد می‌تواند مقادیر دلخواه ذخیره کند (`role = "WIZARD"`)، انواع TS درباره امنیت دروغ می‌گویند، و هیچ محافظ سطح DB در برابر غلط املایی نیست.

**راه‌حل پیشنهادی:**
به enum‌های PostgreSQL تبدیل کنید. نیاز به migration دارد ولی کلاسی از باگ‌ها را حذف می‌کند.

---

### M13. جدول `Task` فاقد `@@unique([studentId, order])` — order تکراری/شکاف مجاز است

- **فایل / خط:** `prisma/schema.prisma:134-169`
- **دسته:** یکپارچگی داده / باگ مرتب‌سازی

**چه خطایی می‌دهد (ساده):**
مرتب‌سازی drag-and-drop مقادیر `order` کلاینت‌ساز را می‌نویسد. schema هیچ محدودیت unique روی `(studentId, order)` ندارد، پس دو تسک برای یک دانش‌آموز می‌توانند `order` یکسان داشته باشند. handler GET با `order: 'asc'` مرتب می‌کند ولی tie‌ها غیرقطعی حل می‌شوند → flicker/مرتب‌سازی مجدد در UI.

**راه‌حل پیشنهادی:**
`@@unique([studentId, order])` اضافه کنید (یا حداقل `@@index([studentId, order])` برای عملکرد — فعلاً هیچ index روی `studentId` نیست).

---

### M14. جدول `Task` هیچ index روی `studentId` (یا `studentId + date`) ندارد

- **فایل / خط:** `prisma/schema.prisma:134-169`
- **دسته:** عملکرد

**چه خطایی می‌دهد (ساده):**
مکررترین کوئری `db.task.findMany({ where: { studentId, date: ... } })` است. PostgreSQL ستون‌های FK را به‌طور خودکار index نمی‌کند. بدون index، با رشد جدول یک full table scan می‌شود.

**راه‌حل پیشنهادی:**
`@@index([studentId])` و `@@index([studentId, date])` به مدل Task اضافه کنید. `@@index([senderId])` را هم به Message اضافه کنید.

---

### M15. `Task.studentId`/`subjectId` از `onDelete: Restrict` استفاده می‌کنند ولی اپ soft-delete دارد

- **فایل / خط:** `prisma/schema.prisma:163-164`
- **دسته:** یکپارچگی داده / ناسازگاری معنای حذف

**چه خطایی می‌دهد (ساده):**
`User` فیلد `deletedAt` برای soft-delete دارد و `Subject` از `isActive: false` استفاده می‌کند. ولی `Task.studentId`/`subjectId` از `onDelete: Restrict` (پیش‌فرض Prisma) دارند. یعنی: هرگز نمی‌توان یک کاربر با تسک را hard-delete کرد (خطا می‌اندازد) و هرگز یک درس با تسک. چون اپ soft-delete در نظر دارد، عمدتاً خوب است — ولی هیچ پشتیبانی `@@deletedAt`/filtered-query وجود ندارد، پس هر کوئری باید دستی `deletedAt: null` و `isActive: true` را فیلتر کند. فراموش کردن این، تسک‌های کاربر soft-delete شده را برمی‌گرداند. `GET /api/tasks` فیلتر `deletedAt: null` روی دانش‌آموز ندارد.

**راه‌حل پیشنهادی:**
یا فیلتر `deletedAt: null` را به تمام کوئری‌های خواندن تسک اضافه کنید، یا از Prisma extension `@deletedAt` استفاده کنید.

---

### M16. `student-utils.ts` به `'s1'` (یک ID mock) برمی‌گردد وقتی کاربر null است

- **فایل / خط:** `src/lib/student-utils.ts:11,20`
- **دسته:** منطق / نشت داده mock

**چه خطایی می‌دهد (ساده):**
`useCurrentStudentId()` مقدار `user?.id || 's1'` را برمی‌گرداند. اگر کاربر لاگین‌نشده باشد، هر کامپوننتی که از این hook استفاده می‌کند روی `'s1'` کار می‌کند — یک ID جعلی که در DB وجود ندارد. فراخوانی‌های API با `studentId: 's1'` با 403/404 برمی‌گردند، ولی هر منطق client-side روی داده mock اجرا می‌شود.

**راه‌حل پیشنهادی:**
وقتی کاربر null است، `null` برگردانید و فراخواننده‌ها را مجبور کنید حالت احراز هویت‌نشده را مدیریت کنند.

---

### M17. `analytics.ts` `buildActivityBreakdown` به دقیقه گرد می‌کند در حالی که `buildDailyTrend` از ساعت با ۱ اعشار استفاده می‌کند

- **فایل / خط:** `src/lib/analytics.ts:319-323` vs `125`
- **دسته:** ناسازگاری / باگ نمایش

**چه خطایی می‌دهد (ساده):**
`buildDailyTrend` مقدار `hours: Math.round((minutes / 60) * 10) / 10` (مثلاً `1.5` ساعت). `buildActivityBreakdown` مقدار `Math.round(datum.مطالعه)` — دقیقه صحیح (مثلاً `90`). هر دو توسط chart‌ها در `AnalyticsView` مصرف می‌شوند. chart میله‌ای مقادیر مثل «90» نشان می‌دهد در حالی که chart روند «1.5» برای همان داده، بدون برچسب واحد در نوع.

**راه‌حل پیشنهادی:**
یک واحد انتخاب کنید و در هر دو تابع استفاده کنید. واحد را در نوع مستند کنید.

---

### M18. `persian-date.ts` `minutesToHoursLabel` خروجی گیج‌کننده برای 1.5 ساعت تولید می‌کند

- **فایل / خط:** `src/lib/persian-date.ts:180-181`
- **دسته:** UX / باگ نمایش

**چه خطایی می‌دهد (ساده):**
برای `minutes = 90`: خروجی `'۱.۱/۲ ساعت'` — litterally «1.1/2 ساعت». این نماد ممیز را با نماد کسر ترکیب می‌کند و رشته‌ای بی‌معنی تولید می‌کند. تست در `persian-date.test.ts:58` همین خروجی را assert می‌کند، پس «طبق تست کار می‌کند» ولی تست باگ را ثبت کرده است.

**راه‌حل پیشنهادی:**
برای حالت `fraction === 0.5`، `'۱.۵ ساعت'` (اعشار فارسی) یا `'۱ ۱/۲ ساعت'` (کسر با فاصله) برگردانید. تست را به‌روزرسانی کنید.

---

### M19. `phone.ts` `normalizeIranianPhone` پیشوند `0098` یا پرانتز را مدیریت نمی‌کند

- **فایل / خط:** `src/lib/phone.ts:12-19`
- **دسته:** edge case ورودی

**چه خطایی می‌دهد (ساده):**
فقط فاصله و خط تیره را strip می‌کند. `00989123456789` هیچ تطبیق پیشوندی نمی‌گیرد، regex شکست می‌خورد و تلفن نامعتبر رد می‌شود. `(0912) 345-6789` بعد از strip `(0912)3456789` می‌شود — پرانتز strip نمی‌شود و شکست می‌خورد.

**راه‌حل پیشنهادی:**
تمام کاراکترهای غیر رقم (به جز `+` ابتدا) را strip کنید (`replace(/[^\d+]/g, '')`)، `0098` را قبل از `98` مدیریت کنید.

---

### M20. `store.ts` مقدار اولیه `exams` با `MOCK_EXAMS` شامل نتایج جعلی

- **فایل / خط:** `src/lib/store.ts:1130`
- **دسته:** نشت داده mock

**چه خطایی می‌دهد (ساده):**
state اولیه `exams` برابر `MOCK_EXAMS` — ۴ آزمون جعلی با نتایج جعلی برای ID‌های دانش‌آموز جعلی. قبل از resolve شدن `loadExams()`، UI این آزمون‌های mock را render می‌کند. اگر فراخوانی API شکست بخورد، کاربر به‌طور نامحدود آزمون‌های جعلی می‌بیند. محاسبه اعلان هم روی `state.exams` تکرار می‌کند، پس اعلان «آزمون پیش‌رو» برای آزمون mock روشن می‌شود.

**راه‌حل پیشنهادی:**
`exams: []` را مقدار اولیه کنید و یک skeleton بارگذاری نشان دهید. اگر API شکست خورد، حالت خطا نشان دهید، نه داده mock.

---

### M21. `store.ts` `instituteAdvisors`/`instituteStudents` به‌طور دائمی داده mock نگه می‌دارند

- **فایل / خط:** `src/lib/store.ts:1045,1055`
- **دسته:** نشت داده mock

**چه خطایی می‌دهد (ساده):**
`instituteAdvisors: MOCK_INSTITUTE_ADVISORS` و `instituteStudents: MOCK_INSTITUTE_STUDENTS` به‌عنوان state اولیه هاردکد شده‌اند. برخلاف تسک/آزمون (که action `loadX` آن‌ها را جایگزین می‌کند)، این آرایه‌ها هرگز با داده API جایگزین نمی‌شوند — action‌ها آرایه mock را درجا جهش می‌دهند. یک مدیر مؤسسه همیشه ۴ مشاور mock و ۱۰ دانش‌آموز mock می‌بیند.

**راه‌حل پیشنهادی:**
به `[]` مقدار اولیه کنید و action‌های `loadInstituteMembers` اضافه کنید.

---

### M22. ESLint تقریباً تمام rule‌ها را غیرفعال کرده — صفر safety net

- **فایل / خط:** `eslint.config.mjs:7-47`
- **دسته:** کیفیت lint

**چه خطایی می‌دهد (ساده):**
۲۴ rule غیرفعال شده، شامل `@typescript-eslint/no-unused-vars`، `react-hooks/exhaustive-deps`، `no-console`، `no-debugger`، `no-unreachable`. `npm run lint` حتی با باگ‌های واضح (متغیر استفاده‌نشده، کد غیرقابل دسترس، `debugger`) pass می‌شود. ESLint عملاً no-op است.

**راه‌حل پیشنهادی:**
حداقل `no-unreachable`، `no-debugger`، `@typescript-eslint/no-unused-vars` را (به‌عنوان warn) دوباره فعال کنید.

---

### M23. وابستگی‌های مرده در `package.json` — در `src/` import نمی‌شوند

- **فایل / خط:** `package.json:54,68,69,88`
- **دسته:** وابستگی / bloat

**چه خطایی می‌دهد (ساده):**
چهار وابستگی در `src/` import نمی‌شوند: `@reactuses/core`، `next-auth` (اپ از توکن HMAC سفارشی استفاده می‌کند نه next-auth)، `next-intl`، `z-ai-web-dev-sdk`. این‌ها `node_modules` را متورم می‌کنند، نصب/build را کند می‌کنند و حجم image Liara را افزایش می‌دهند.

**راه‌حل پیشنهادی:**
`npm uninstall @reactuses/core next-auth next-intl z-ai-web-dev-sdk` (اول dynamic import چک کنید).

---

### M24. `reactStrictMode: false` ممکن است باگ‌های افکت را پنهان کند

- **فایل / خط:** `next.config.ts:9`
- **دسته:** کیفیت

**چه خطایی می‌دهد (ساده):**
React Strict Mode در dev افکت‌ها/رندرها را دوبار فراخوانی می‌کند تا باگ‌های اثر جانبی (stale closure، cleanup مفقود، رندر ناخالص) را آشکار کند. غیرفعال کردن یعنی این باگ‌ها فقط در production نمایان می‌شوند که دیباگ سخت‌تر است.

**راه‌حل پیشنهادی:**
`reactStrictMode: true` (پیش‌فرض Next.js) ست کنید و باگ‌های آشکار شده را برطرف کنید.

---

### M25. `setTimeout` در `CurriculumWizard` با index قدیمی closure می‌سازد — ردیف اشتباه پاک می‌شود

- **فایل / خط:** `src/components/super-admin/CurriculumWizard.tsx:512-516` و `703-710`
- **دسته:** stale closure

**چه خطایی می‌دهد (ساده):**
اگر کاربر ردیف ۲ را ذخیره کند، قبل از ۱.۵ ثانیه یک ردیف اضافه/حذف کند، `idx` دیگر به همان ردیف اشاره نمی‌کند — badge «ذخیره شد» روی ردیف اشتباه پاک می‌شود. تایم‌اوت هم در unmount پاک نمی‌شود.

**راه‌حل پیشنهادی:**
ردیف ذخیره شده را با `id` (نه ایندکس آرایه) داخل تایم‌اوت پیگیری کنید.

---

### M26. تنظیمات state محلی با تغییر store user همگام نمی‌شود

- **فایل / خط:** `src/components/settings/SettingsView.tsx:485-493` و `src/components/institute/InstituteSettings.tsx:21-22`
- **دسته:** state قدیمی / همگام‌سازی مفقود

**چه خطایی می‌دهد (ساده):**
اگر آبجکت user از تب دیگری یا از طریق رفرش API پس‌زمینه به‌روزرسانی شود، فرم تنظیمات همچنان نام/پایه قدیمی را نشان می‌دهد. کاربر فکر می‌کند تغییراتش ذخیره نشده‌اند.

**راه‌حل پیشنهادی:**
یک `useEffect(() => { setDisplayName(user?.name ?? ''); ... }, [user])` اضافه کنید تا دوباره همگام شود.

---

### M27. action‌های store از `fetch` خام به جای `apiFetch` استفاده می‌کنند — دور زدن handler 401

- **فایل / خط:** `src/lib/store.ts:925` (`loadAdvisorStudents`)، `1073-1098` (CRUD مؤسسه)، `1102-1124` (CRUD کاربر)
- **دسته:** مدیریت احراز هویت ناسازگار

**چه خطایی می‌دهد (ساده):**
وقتی نشست منقضی می‌شود، این فراخوانی‌ها 401 برمی‌گردانند ولی `unauthHandler` سراسری را فعال نمی‌کنند. کاربر toast‌های خطای فردی می‌بیند به جای هدایت زیبا به لاگین.

**راه‌حل پیشنهادی:**
`fetch` خام را در تمام متدهای store با `apiFetch` جایگزین کنید.

---

### M28. `connection-requests` POST اجازه باز کردن مجدد درخواست REJECTED/ENDED را می‌دهد

- **فایل / خط:** `src/app/api/connection-requests/route.ts:43-48`
- **دسته:** منطق / آزار

**چه خطایی می‌دهد (ساده):**
`upsert` روی `studentId_advisorId` کورکورانه `status: 'PENDING'` و `respondedAt: null` را ست می‌کند. اگر یک مشاور درخواست دانش‌آموز را REJECT کرده باشد، دانش‌آموز می‌تواند بلافاصله دوباره درخواست دهد (بدون cooldown، بدون چک state) و مشاور را بمباران کند.

**راه‌حل پیشنهادی:**
قبل از upsert، `status` موجود را چک کنید. باز کردن مجدد را رد کنید اگر `REJECTED` (یا cooldown اعمال کنید).

---

### M29. Message broadcast یک ردیف `Message` به‌ازای هر دانش‌آموز در یک تراکنش می‌سازد

- **فایل / خط:** `src/app/api/messages/route.ts:159-170`
- **دسته:** عملکرد / احتمال timeout

**چه خطایی می‌دهد (ساده):**
`db.$transaction(assignedStudents.map(s => db.message.create({...})))` — برای مشاوری با ۵۰۰ دانش‌آموز، ۵۰۰ INSERT در یک تراکنش. تراکنش‌های طولانی lock نگه می‌دارند و می‌توانند timeout شوند. `assignedStudents` هم بدون `take` fetch می‌شود.

**راه‌حل پیشنهادی:**
لیست دانش‌آموزان را محدود کنید، insert‌ها را batch کنید (`createMany` با chunk)، یا از یک ردیف broadcast با `recipientId: null` استفاده کنید (schema پشتیبانی می‌کند).

---

### M30. `spaced-repetition.ts` از SM-2 منحرف می‌شود: ease factor روی کارت‌های فراموش‌شده به‌روز نمی‌شود

- **فایل / خط:** `src/lib/spaced-repetition.ts:82-86`
- **دسته:** انحراف الگوریتم

**چه خطایی می‌دهد (ساده):**
SM-2 استاندارد ease factor را برای تمام مقادیر quality به‌روز می‌کند. این پیاده‌سازی فقط برای `quality >= 3` (یادآوری درست) به‌روز می‌کند. برای `quality < 3` (فراموشی)، EF تغییر نمی‌کند. نتیجه: کارتی که مکرراً فراموش می‌شود هرگز آسان‌تر نمی‌شود (فاصله‌های کوتاه‌تر)، پس دانش‌آموز آن را در همان فاصله در حال رشد برای همیشه می‌بیند.

**راه‌حل پیشنهادی:**
اگر وفاداری SM-2 مطلوب است، فرمول EF را برای تمام مقادیر quality اعمال کنید. در غیر این صورت، انحراف را مستند کنید.

---

### M31. `otp.ts` هیچ rate limiting مبتنی بر IP ندارد؛ cooldown فقط per-phone است

- **فایل / خط:** `src/lib/otp.ts:10-17`
- **دسته:** امنیت / vector آزار

**چه خطایی می‌دهد (ساده):**
`requestOtp` cooldown ۶۰ ثانیه‌ای per-phone اعمال می‌کند. ولی یک مهاجم می‌تواند از یک IP روی شماره‌های زیادی درخواست OTP بفرستد (هزینه واقعی پول برای SMS-IR) و ممکن است SMS provider را DoS کند. `verifyOtp` ۵ تلاش per challenge بدون throttle مبتنی بر IP اجازه می‌دهد.

**راه‌حل پیشنهادی:**
rate limiting مبتنی بر IP اضافه کنید (مثلاً max ۱۰ درخواست OTP per IP per hour). بعد از ۳ درخواست از یک IP CAPTCHA در نظر بگیرید.

---

### M32. `Toggle` داخل کامپوننت والد تعریف شده — هر render remount می‌شود

- **فایل / خط:** `src/components/super-admin/SuperAdminSettings.tsx:38-47`
- **دسته:** عملکرد / remount

**چه خطایی می‌دهد (ساده):**
چون `Toggle` یک رفرنس تابع جدید در هر render است، React کل زیردرخت toggle را در هر تغییر state unmount/remount می‌کند. focus از دست می‌رود، transition‌ها خراب می‌شوند و flicker قابل مشاهده است.

**راه‌حل پیشنهادی:**
`Toggle` را به بیرون والد منتقل کنید یا JSX را inline کنید.

---

### M33. تیکت‌ها فقط محلی هستند — هرگز به سرور persist نمی‌شوند

- **فایل / خط:** `src/components/settings/SettingsView.tsx:522-540` و `src/lib/store.ts:1009`
- **دسته:** از دست رفتن داده

**چه خطایی می‌دهد (ساده):**
فرم «ایجاد تیکت» یک تیکت در store محلی Zustand می‌سازد (با `MOCK_TICKETS` seed شده). در رفرش صفحه یا خروج، تمام تیکت‌های ساخته‌شده کاربر ناپدید می‌شوند. کاربر فکر می‌کند درخواست پشتیبانی‌اش ارسال شده ولی هرگز ارسال نشده.

**راه‌حل پیشنهادی:**
یک فراخوانی API (`/api/tickets`، POST) در `addTicket` اضافه کنید.

---

### M34. `SuperAdminDashboard` خطاهای بارگذاری را بی‌صدا بلعیده، بدون state بارگذاری

- **فایل / خط:** `src/components/super-admin/SuperAdminDashboard.tsx:26`
- **دسته:** state بارگذاری/خطا مفقود

**چه خطایی می‌دهد (ساده):**
در mount، داشبورد «۰ موسسات فعال» (آرایه خالی) بدون spinner و بدون نشانگر خطا نشان می‌دهد. اگر API شکست بخوردد (403، 500، شبکه)، کاربر یک داشبورد با صفر دائمی بدون بازخورد می‌بیند.

**راه‌حل پیشنهادی:**
state‌های `loading` و `error` را پیگیری کنید؛ skeleton هنگام بارگذاری و دکمه retry در خطا نشان دهید.

---

### M35. `MusicPlayer` state پخش را toggle می‌کند ولی صدا پخش نمی‌کند

- **فایل / خط:** `src/components/shared/MusicPlayer.tsx`
- **دسته:** قابلیت شکسته / mock باقی‌مانده

**چه خطایی می‌دهد (ساده):**
دکمه پخش/مکث equalizer را انیمیشن می‌دهد و `isPlaying` را toggle می‌کند، ولی هیچ المان صوتی وجود ندارد. کاربر پخش را می‌زند و چیزی نمی‌شنود. (توجه: `StudyMusicPlayer.tsx` در ToolsHub به‌درستی از Web Audio API استفاده می‌کند.)

**راه‌حل پیشنهادی:**
یا به همان موتور Web Audio متصل کنید، یا پلیر شناور را حذف کنید.

---

### M36. `DataExportHelper` شنونده رویداد را در هر تغییر `tasks` دوباره ثبت می‌کند

- **فایل / خط:** `src/components/shared/DataExportHelper.tsx:71-153`
- **دسته:** افکت ناکارآمد

**چه خطایی می‌دهد (ساده):**
هر افزودن/به‌روزرسانی/حذف تسک باعث removeEventListener + addEventListener می‌شود. اگر کاربر در میکروثانیه gap صادر کند، از دست می‌رود. CPU بی‌مورد در هر فشردن کلید.

**راه‌حل پیشنهادی:**
`tasks`/`user` را در یک ref ذخیره و از `[]` به‌عنوان deps استفاده کنید.

---

### M37. شنونده `useToast` در هر تغییر state دوباره ثبت می‌شود

- **فایل / خط:** `src/hooks/use-toast.ts:177-185`
- **دسته:** افکت ناکارآمد

**چه خطایی می‌دهد (ساده):**
نسخه استاندارد shadcn `[]` deps دارد. این نسخه تغییر یافته افکت را در هر toast (چون `state` تغییر می‌کند) دوباره اجرا می‌کند. در gap cleanup/re-add، یک toast ارسال‌شده ممکن است این subscriber را از دست بدهد.

**راه‌حل پیشنهادی:**
deps را به `[]` تغییر دهید.

---

### M38. `getPersianWeekday` ادعا می‌کند «timezone-independent» ولی از timezone محلی استفاده می‌کند

- **فایل / خط:** `src/lib/persian-date.ts:36-44`
- **دسته:** باگ timezone پنهان

**چه خطایی می‌دهد (ساده):**
کامنت می‌گوید timezone-independent است ولی `toJalaali` از `date.getFullYear()/getMonth()/getDate()` (مؤلفه‌های محلی) می‌خواند. رفت‌وبرگشت Jalali یک no-op است. برای کاربری در UTC-8 ساعت ۱۰ شب، `date.getDay()` دوشنبه برمی‌گرداند در حالی که روز ایران چهارشنبه است. این باعث می‌شود اعلان‌های هدف هفتگی و محدوده‌های آنالیز غلط محاسبه شوند.

**راه‌حل پیشنهادی:**
اگر محاسبه زمان ایران مطلوب است، اول به `Asia/Tehran` تبدیل کنید. در غیر این صورت کامنت را اصلاح کنید.

---

### M39. `computeInsights` «کمترین مطالعه» می‌تواند با «بیشترین مطالعه» برابر شود

- **فایل / خط:** `src/lib/analytics.ts:378-389`
- **دسته:** منطق / UX گیج‌کننده

**چه خطایی می‌دهد (ساده):**
اگر فقط یک درس داده مطالعه داشته باشد، `most === least` (همان درس، همان دقیقه). UI همزمان «بیشترین مطالعه: ریاضی - ۵ ساعت» و «کمترین مطالعه: ریاضی - ۵ ساعت» نشان می‌دهد.

**راه‌حل پیشنهادی:**
وقتی `nonZero.length <= 1` است، `leastStudied` را به placeholder «داده‌ای نیست» ست کنید.

---

### M40. `isValidTaskPageRange` اجازه `pageEnd` بدون `pageStart` می‌دهد، برخلاف `validatePageRange`

- **فایل / خط:** `src/lib/api-auth.ts:292-295` vs `src/lib/validators/page-range.ts:79-85`
- **دسته:** اعتبارسنجی ناسازگار

**چه خطایی می‌دهد (ساده):**
`isValidTaskPageRange` (تسک) `{ pageStart: null, pageEnd: 5 }` را معتبر می‌پذیرد. ولی `validatePageRange` (فصل/گفتار) صراحت «صفحه پایان بدون صفحه شروع مجاز نیست» را رد می‌کند. ناسازگاری یعنی تسک‌ها می‌توانند `pageEnd` معلق داشته باشند.

**راه‌حل پیشنهادی:**
`!(end != null && start == null)` را به `isValidTaskPageRange` اضافه کنید.

---

### M41. `mini-services/` خالی است — اسکریپت‌های `.zscripts` mini-services کد مرده‌اند

- **فایل / خط:** `mini-services/.gitkeep` + `.zscripts/mini-services-*.sh` و `dev.sh:31-81`
- **دسته:** کد مرده / سردرگمی پیکربندی

**چه خطایی می‌دهد (ساده):**
دایرکتوری `mini-services/` فقط `.gitkeep` دارد. هر سه اسکریپت `.zscripts/mini-services-*.sh` روی `mini-services/*/package.json` تکرار می‌کنند و چیزی پیدا نمی‌کنند، پس return/skip می‌کنند. حدود ۲۰۰ خط shell مرده.

**راه‌حل پیشنهادی:**
یا `mini-services/` را با سرویس‌های واقعی پر کنید، یا `.gitkeep` و تمام اسکریپت‌های mini-services را حذف کنید.

---

### M42. `examples/websocket/` پکیج‌هایی import می‌کند که در `package.json` نیستند

- **فایل / خط:** `examples/websocket/frontend.tsx:4` (`socket.io-client`) و `examples/websocket/server.ts:2` (`socket.io`)
- **دسته:** وابستگی / مثال‌های شکسته

**چه خطایی می‌دهد (ساده):**
نه `socket.io` و نه `socket.io-client` در `package.json` هستند. مثال‌ها از `tsconfig` و ESLint مستثنی‌اند پس build را نمی‌شکنند، ولی کد غیرعملکردی است.

**راه‌حل پیشنهادی:**
یا deps را اضافه کنید، یا یک کامنت در ابتدای هر فایل اضافه کنید که پکیج‌های لازم را بیان کند.

---

### M43. `dev` از `tee` استفاده می‌کند — ناسازگار با Windows

- **فایل / خط:** `package.json:6`
- **دسته:** اسکریپت / کراس‌پلتفرم

**چه خطایی می‌دهد (ساده):**
`tee` یک ابزار Unix است. در Windows `npm run dev` با «'tee' is not recognized» شکست می‌خورد.

**راه‌حل پیشنهادی:**
از `next dev -p 3000` استفاده کنید و به logging خود Next.js تکیه کنید.

---

### M44. `exam.subject` رشته آزاد است، نه FK به جدول `Subject`

- **فایل / خط:** `src/app/api/exams/route.ts:121`
- **دسته:** یکپارچگی داده

**چه خطایی می‌دهد (ساده):**
هر رشته‌ای ذخیره می‌شود. جدول `Subject` وجود دارد و توسط تسک استفاده می‌شود، ولی آزمون‌ها یک رشته denormal بدون FK ذخیره می‌کنند. غلط املایی یا تغییر نام دروس منتقل نمی‌شود.

**راه‌حل پیشنهادی:**
در برابر `db.subject.findUnique` اعتبارسنجی کنید و هم `subjectId` و هم نام را ذخیره کنید.

---

### M45. `Task.order` به‌عنوان عدد صحیح اعتبارسنجی نمی‌شود

- **فایل / خط:** `src/app/api/tasks/route.ts:36`
- **دسته:** اعتبارسنجی

**چه خطایی می‌دهد (ساده):**
فقط `typeof body.order !== 'number'` چک می‌شود. اعشار (`1.5`) و مقادیر افراطی پذیرفته می‌شوند ولی ستون `Int` است → Prisma اعشار را رد می‌کند → 500.

**راه‌حل پیشنهادی:**
`Number.isInteger(body.order) && body.order >= 0` اضافه کنید.

---

# بخش ۴ — باگ‌های کم‌اهمیت (LOW)

این موارد کیفیت کد و تجربه کاربری را بهبود می‌بخشند ولی عملکرد را شکسته نمی‌کنند.

---

### L1. ردیف‌های `OtpChallenge` هرگز پاک نمی‌شوند

- **فایل / خط:** `src/lib/otp.ts`
- **دسته:** رشد ذخیره‌سازی

**چه خطایی می‌دهد (ساده):**
ردیف‌های منقضی و مصرف‌شده هرگز حذف نمی‌شوند. جدول به‌مرور بی‌حد رشد می‌کند.

**راه‌حل پیشنهادی:**
یک cleanup زمان‌بندی‌شده اضافه کنید (یا `deleteMany({ where: { expiresAt: { lt: new Date() } }})` در هر درخواست).

---

### L2. `toPersianDigits` در ۷+ فایل تکرار شده

- **فایل / خط:** `Dashboard.tsx:19`، `InstituteDashboard.tsx:21`، `SuperAdminDashboard.tsx:19`، `SuperAdminUsers.tsx:26`، `SuperAdminSettings.tsx:18`، `TaskSubjectPicker.tsx:38`، `study-session-store.ts:225`
- **دسته:** کد تکراری

**راه‌حل پیشنهادی:**
از `@/lib/persian-date` import کنید.

---

### L3. رشته جادویی `'s1'` برای دانش‌آموز دمو

- **فایل / خط:** `src/components/dashboard/Dashboard.tsx:227`
- **دسته:** شکننده/مستند نشده

**راه‌حل پیشنهادی:**
با یک ثابت نام‌گذاری شده جایگزین کنید.

---

### L4. `setTimeout` چندتایی در unmount پاک نمی‌شوند

- **فایل / خط:** `SuperAdminSettings.tsx:34`، `InstituteSettings.tsx:33`، `CurriculumWizard.tsx:418,512,594,703`
- **دسته:** code smell

**راه‌حل پیشنهادی:**
ID تایم‌اوت را ذخیره و در cleanup پاک کنید.

---

### L5. `pagehide` + `visibilitychange` هر دو `pauseAll()` صدا می‌زنند — مکرر

- **فایل / خط:** `src/components/plan/StudySessionTimer.tsx:87-101`
- **دسته:** مکرر (بی‌ضرر)

**راه‌حل پیشنهادی:**
یک‌بار اجرا را تضمین کنید.

---

### L6. `LoadingScreen` صفحه فرود ۹۰۰ms تأخیر مصنوعی اضافه می‌کند

- **فایل / خط:** `src/components/landing/loading-screen.tsx:18`
- **دسته:** آزار جزئی UX

**راه‌حل پیشنهادی:**
تأخیر را حذف کنید یا فقط در بارگذاری اول نشان دهید.

---

### L7. `SuperAdminDashboard` chart «رشد» فقط ماه فعلی را نشان می‌دهد

- **فایل / خط:** `src/components/super-admin/SuperAdminDashboard.tsx:196`
- **دسته:** گمراه‌کننده

**چه خطایی می‌دهد (ساده):**
chart «رشد پلتفرم (ماهانه)» یک آرایه تک‌عنصری است. برچسب «ماهانه» گمراه‌کننده است.

**راه‌حل پیشنهادی:**
داده تاریخی اضافه کنید یا برچسب را تغییر دهید.

---

### L8. `confirm()` برای عملیات مخرب در `CurriculumWizard` استفاده شده

- **فایل / خط:** `src/components/super-admin/CurriculumWizard.tsx:528,728`
- **دسته:** UX

**چه خطایی می‌دهد (ساده):**
`window.confirm` thread اصلی را مسدود می‌کند و ناهماهنگ با بقیه UI است.

**راه‌حل پیشنهادی:**
از shadcn `AlertDialog` استفاده کنید.

---

### L9. `.gitignore` ورودی‌های مکرر و بیش از حد عمومی دارد

- **فایل / خط:** `.gitignore:47,49,58`
- **دسته:** بهداشت گیت

**چه خطایی می‌دهد (ساده):**
`dev.log` در خط ۴۷ و ۵۸ تکرار شده. خط ۴۹ `test` (بدون اسلش) هر فایل یا دایرکتوری به نام `test` را نادیده می‌گیرد.

**راه‌حل پیشنهادی:**
مکرر را حذف کنید. `test` را به `/test` تغییر دهید.

---

### L10. `target: "ES2017"` در tsconfig قدیمی است

- **فایل / خط:** `tsconfig.json:3`
- **دسته:** کیفیت

**راه‌حل پیشنهادی:**
به `"ES2022"` ارتقا دهید.

---

### L11. `output: "standalone"` روی Liara غیرضروری است

- **فایل / خط:** `next.config.ts:4`
- **دسته:** هدررفت منابع

**چه خطایی می‌دهد (ساده):**
Liara از `next start` استفاده می‌کند که از `.next` معمولی استفاده می‌کند، نه standalone. build standalone تولید می‌شود ولی هرگز استفاده نمی‌شود.

**راه‌حل پیشنهادی:**
حذف کنید مگر اینکه به deployment server سفارشی بخواهید.

---

### L12. `liara.json` فاقد `build.command` است

- **فایل / خط:** `liara.json:4-7`
- **دسته:** رفتار ضمنی

**چه خطایی می‌دهد (ساده):**
اگر Liara به‌جای `npm run build` مستقیماً `next build` اجرا کند، `prisma generate` اجرا نمی‌شود و import‌های `@prisma/client` با «PrismaClient is not defined» شکست می‌خورند.

**راه‌حل پیشنهادی:**
`"build": { "command": "npm run build", ... }` را صریح ست کنید.

---

### L13. متن گمراه‌کننده در فرم لاگین

- **فایل / خط:** `src/components/auth/LoginPage.tsx:169`
- **دسته:** کپی

**چه خطایی می‌دهد (ساده):**
«شماره تلفن و رمز عبور را وارد کنید» ولی فرم از OTP استفاده می‌کند نه رمز عبور.

**راه‌حل پیشنهادی:**
متن را اصلاح کنید.

---

### L14. ورودی تلفن ارقام فارسی را strip نمی‌کند

- **فایل / خط:** `src/components/auth/LoginPage.tsx:184`
- **دسته:** edge case ورودی

**چه خطایی می‌دهد (ساده):**
کاربران فارسی‌زبان اگر ارقام فارسی (۰۹۱۲…) تایپ کنند، چک `/^\d{6}$/` در submit شکست می‌خورد. OnboardingWizard این را درست مدیریت می‌کند.

**راه‌حل پیشنهادی:**
ارقام فارسی را به لاتین تبدیل کنید (مثل `normalizeIranianPhone`).

---

### L15. آیکن‌های دکمه‌ای بدون `aria-label` پراکنده

- **فایل / خط:** `src/components/shared/NotificationCenter.tsx` (آیتم‌ها)
- **دسته:** دسترس‌پذیری

**راه‌حل پیشنهادی:**
`aria-label` صریح اضافه کنید.

---

### L16. `formatNextReview` از `Math.round(days / 30)` استفاده می‌کند — «۱ ماه» برای ۲۲ روز نشان می‌دهد

- **فایل / خط:** `src/lib/spaced-repetition.ts:204`
- **دسته:** نادرستی نمایش جزئی

**راه‌حل پیشنهادی:**
`Math.floor` و فقط ماه‌ها را وقتی `days >= 30` نشان دهید.

---

### L17. `sync-curriculum-from-csv.ts` edge case `chapterBounds`

- **فایل / خط:** `prisma/sync-curriculum-from-csv.ts:60-68`
- **دسته:** edge case منطق

**چه خطایی می‌دهد (ساده):**
اگر یک ردیف `pageEnd: null` («تا پایان کتاب») داشته باشد ولی ردیف بعدی `pageEnd` غیرnull داشته باشد، `Math.max(null, x)` به `x` ارزیابی می‌شود و flag «تا پایان» بی‌صدا گم می‌شود.

**راه‌حل پیشنهادی:**
وقتی `pageEnd` به null ست شد، دیگر بازنویسی نکنید.

---

### L18. عدم ثابت بودن معنای `Task.completed` برای `INCOMPLETE`

- **فایل / خط:** `src/lib/types.ts:29,57`
- **دسته:** ناسازگاری طراحی نوع

**چه خطایی می‌دهد (ساده):**
`INCOMPLETE` و `PENDING` هر دو به `completed: null` نگاشت می‌شوند. کدی که `task.completed` را می‌خواند تا «این انجام شد؟» تصمیم بگیرد، `INCOMPLETE` را «pending» تلقی می‌کند که ممکن است منطق درست نباشد.

**راه‌حل پیشنهادی:**
تمام چک‌های «این تسک انجام شد؟» از `status === 'COMPLETED'` استفاده کنند، نه `completed === true`.

---

### L19. `Caddyfile` روی `:81` — فقط sandbox

- **فایل / خط:** `Caddyfile:1`
- **دسته:** sandbox فقط

**راه‌حل پیشنهادی:**
مستند کنید که `:81` مختص sandbox است.

---

### L20. `seed-production.ts` تمیز و idempotent است

- **فایل / خط:** `prisma/seed-production.ts`
- **دسته:** (تمیز)

بدون باگ.

---

# جدول خلاصه و اولویت‌بندی

| ردیف | شدت | عنوان کوتاه | فایل اصلی | اولویت |
|------|-----|-------------|-----------|--------|
| C1 | CRITICAL | middleware به proxy تغییر نام → همه API 401 | `src/proxy.ts` | ۱ (فوری) |
| C2 | CRITICAL | `.env` SQLite ولی schema PostgreSQL | `.env` | ۲ |
| C3 | CRITICAL | `AUTH_SECRET` در production تنظیم نشده | `auth.ts:8` | ۳ |
| C4 | CRITICAL | migration‌ها روی Liara اجرا نمی‌شوند | `liara.json` | ۴ |
| C5 | CRITICAL | IDOR ویرایش آزمون | `exams/[id]/route.ts:76` | ۵ |
| C6 | CRITICAL | seed کل جدول task را پاک می‌کند | `seed-from-excel.js:226` | ۶ |
| C7 | CRITICAL | مقایسه HMAC غیر زمان‌مند | `auth.ts:57` | ۷ |
| C8 | CRITICAL | اعتماد به header قابل‌جعل | `api-auth.ts:29` | ۸ |
| C9 | CRITICAL | جهش مستقیم store در drag-and-drop | `SortableTaskList.tsx:122` | ۹ |
| C10 | CRITICAL | `getState()` در render | `PlanView.tsx:566` | ۱۰ |
| C11 | CRITICAL | mount با state احراز قدیمی | `page.tsx:36` | ۱۱ |
| C12 | CRITICAL | حلقه بی‌نهایت انتقال تسک | `PlanView.tsx:130` | ۱۲ |
| C13 | CRITICAL | seed-history status غلط | `seed-history.js:85` | ۱۳ |
| H1–H33 | HIGH | (۳۳ باگ) | متنوع | دسته‌بندی |
| M1–M45 | MEDIUM | (۴۵ باگ) | متنوع | دسته‌بندی |
| L1–L20 | LOW | (۲۰ مورد) | متنوع | اختیاری |

---

## توصیه‌های نهایی

### ترتیب پیشنهادی برای برطرف کردن (Roadmap)

**مرحله ۱ — بازگرداندن قابلیت استفاده (فوری):**
1. C1: `src/proxy.ts` → `src/middleware.ts` (بازگرداندن نام). این به‌تنهایی ۹۰٪ اپ را دوباره فعال می‌کند.
2. C2: `.env` را از گیت حذف (`git rm --cached .env`) و `DATABASE_URL` واقعی PostgreSQL در Liara تنظیم کنید.
3. C3: `AUTH_SECRET` را در Liara تنظیم کنید.
4. C4: `release`/`build.command` در `liara.json` اضافه کنید تا migration و seed اجرا شوند.

**مرحله ۲ — بستن حفره‌های امنیتی:**
5. C5: اعتبارسنجی مالکیت `studentIds` در PATCH آزمون.
6. C7 + C8: مقایسه زمان‌مند HMAC و defense-in-depth در `requireAuth`.
7. H1: محافظت CSRF.
8. H2 + H17: دور زدن OTP sandbox را فقط محدود به dev کنید.
9. H6: جلوگیری از حذف خود توسط مدیر کل.

**مرحله ۳ — یکپارچگی داده:**
10. C6: حذف `task.deleteMany({})` از seed-from-excel.
11. C13 + H19 + H20 + H22: اصلاح اسکریپت‌های seed (idempotent، بدون ID هاردکد، status درست).
12. H5: مدیریت وابستگی‌ها هنگام حذف کاربر.
13. H7 + H28: رفع شرایط مسابقه و state machine درخواست اتصال.
14. H25: پاکسازی ExamResult یتیم هنگام حذف شرکت‌کننده.
15. M13 + M14: افزودن `@@unique` و `@@index` به مدل Task.

**مرحله ۴ — پایداری frontend:**
16. C9 + C10 + C11 + C12: رفع جهش state، خواندن ری‌اکتیو، mount stale، حلقه بی‌نهایت.
17. H11 + H15: streak بعد از تأیید؛ debounce ورودی.
18. H14 + H16 + M36 + M37: پاکسازی شنونده‌ها و تایم‌اوت‌ها.

**مرحله ۵ — عملکرد:**
19. H8 + H26 + M2 + M4 + M5: صفحه‌بندی و aggregate به‌جای بارگذاری نامحدود.

**مرحله ۶ — کیفیت کد و پیکربندی:**
20. H27 + H28 + H30–H33 + M22–M24 + M41–M43: پاکسازی lockfile، اسکریپت‌ها، ESLint، deps مرده.

---

### نکات کلیدی برای استقرار Liara

1. `git rm --cached .env` و در پنل Liara این متغیرها را تنظیم کنید:
   - `DATABASE_URL` (رشته PostgreSQL واقعی)
   - `AUTH_SECRET` (رشته تصادفی قوی)
   - `SMS_IR_API_KEY`، `SMS_IR_OTP_TEMPLATE_ID`، اختیاری `SMS_IR_MODE`
   - `INITIAL_SUPER_ADMIN_PHONES` (یا `INITIAL_SUPER_ADMIN_PHONE`)
   - `INITIAL_SUPER_ADMIN_NAME`
2. در `liara.json` اضافه کنید:
   ```json
   "build": { "command": "npm run build", "location": "germany", "path": "." },
   "release": "npm run liara:release"
   ```
   (اگر Liara از `release` پشتیبانی می‌کند؛ در غیر این صورت بعد از deploy دستی اجرا کنید.)
3. یکی از lockfile‌ها را حذف کنید (با npm deploy → `package-lock.json` نگه دارید، `bun.lock` را حذف).
4. مسیرهای `/home/z/my-project/...` هاردکد شده را از اسکریپت‌ها حذف کنید.

---

### جمع‌بندی

این پروژه در کامیت فعلی **به‌دلیل باگ C1 (تغییر نام middleware) عملاً غیرقابل‌استفاده است** — هر درخواست API محافظت‌شده پس از لاگین 401 برمی‌گرداند. این یک باگ یک‌خطی است اما اثر آن مهارکننده است. باگ‌های بحرانی دیگر (C2–C13) شامل نشت `.env`، عدم تنظیم env در production، IDOR آزمون، از دست رفتن داده در seed، و چند باگ frontend جدی هستند که باید قبل از هر استقرار واقعی برطرف شوند. مجموعاً **۱۳ باگ بحرانی، ۳۳ باگ با اهمیت بالا، ۴۵ باگ متوسط و ۲۰ مورد کم‌اهمیت** شناسایی شد.

**توصیه:** ابتدا باگ C1 را برطرف کنید تا اپ کار کند، سپس طبق roadmap بالا پیش بروید.

---

*پایان گزارش. این گزارش فقط باگ‌ها را مستند می‌کند و هیچ کد اصلاحی نمی‌نویسد. منتظر دستور بعدی شما هستم.*
