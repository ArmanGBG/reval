هدف اصلی: اضافه کردن فیچر «فعالیت‌های غیردرسی» (Non-Study Activities) و یکپارچه‌سازی آن با سیستم گزارش‌گیری، با رعایت کامل اصول ریسپانسیو (Mobile-First).

۱. دکمه‌های عملیاتی و فرم ثبت (Modal)

دکمه جدید: افزودن دکمه سوم به نام «فعالیت غیردرسی» زیر دکمه‌های «تسک جدید» و «آزمون جدید». استفاده از رنگ‌بندی ملایم و متمایز (مثلاً bg-blue-50 text-blue-600) و یک آیکون اختصاصی جهت جلوگیری از رقابت بصری با تسک‌های آموزشی. در نمای موبایل، این دکمه‌ها باید به صورت تمام‌عرض (Full-width) یا با پدینگ مناسب برای لمس (Touch Target) رندر شوند.

فرم ثبت (Modal): فیلدهای این فرم شامل موارد زیر است:

نوع فعالیت (اجباری): انتخاب از دسته‌بندی‌های زیر به همراه آیکون: 📱 رسانه و فضای مجازی، 🎮 بازی و سرگرمی، ☕ گردش و تعاملات، 🏃‍♂️ سلامت و ورزش، 🌱 مهارت‌های فردی.

مدت زمان (اختیاری - بسیار مهم): فیلد ورودی زمان صرف‌شده به دقیقه. این فیلد باید کاملاً Optional باشد و سیستم در صورت خالی بودن آن خطا ندهد.

نمایش مدال در موبایل: این مدال باید در صفحات کوچک (sm و کوچکتر) به‌صورت کامل ریسپانسیو عمل کند (مثلاً تبدیل به Bottom Sheet شود یا حاشیه‌های آن برای موبایل بهینه گردد).

۲. تب اختصاصی و گزارش‌گیری نقطه‌ای

تب جدید: اضافه شدن تب «فعالیت‌های غیردرسی» در نویگیشن، کنار تب‌های «برنامه روز» و «آزمون‌ها». الزام ریسپانسیو: منوی تب‌ها در موبایل باید قابلیت اسکرول افقی (Horizontal Scroll) داشته باشد تا ساختار صفحه نشکند.

فیلتر و محاسبه: در این تب، منطق انتخاب بازه زمانی (روزانه، هفتگی، سفارشی) باید دقیقاً مشابه کامپوننت‌های موجود در بخش گزارش درسی پیاده شود. مجموع تایم صرف‌شده برای هر دسته‌بندی بر اساس بازه انتخابی به‌دقت محاسبه و نمایش داده شود.

۳. یکپارچگی با گزارش جامع (گزارش درسی / گزارش کامل)

نمای کلی فعالیت‌ها: در بخش اصلیِ گزارش جامع درسی، یک ویجت یا نمای کلی (Overview) از فعالیت‌های غیردرسی اضافه شود. در حالت موبایل، چیدمان این ویجت‌ها باید به صورت ستونی (Stack) و در دسکتاپ به صورت ردیفی (Grid/Flex) باشد.

اعمال دقیق بازه زمانی (حیاتی): ایجنت باید اطمینان حاصل کند که state مربوط به بازه زمانی انتخاب‌شده (Date Range) به‌طور سراسری و صحیح کار می‌کند؛ به‌طوری‌که با تغییر بازه در گزارش جامع، دیتای نمای کلیِ فعالیت‌های غیردرسی نیز فوراً و دقیقاً بر اساس همان بازه فیلتر شود.

۴. رندر کردن در تب اصلی (برنامه روز)

نمایش ترکیبی: در تب پیش‌فرض «برنامه روز»، این فعالیت‌ها لابه‌لای تسک‌های درسی رندر شوند.

استایل متمایز و ریسپانسیو: طراحی کارتِ این فعالیت‌ها باید کاملاً با تسک‌های آموزشی متفاوت باشد. از پس‌زمینه خنثی/ملایم، حاشیه نقطه‌چین (Dashed Border) و آیکون اختصاصی دسته‌بندی استفاده شود. این کارت‌ها نباید چک‌باکس تکمیل وظیفه داشته باشند. سایز فونت‌ها و فواصل (Padding/Margin) کارت‌ها باید با کلاس‌های Tailwind برای سایزهای مختلف مانیتور و موبایل بهینه‌سازی شود.